// === GENERATED by sqlink-shim-codegen (target=duckdb)  do not edit by hand ===

//! Generated by sqlink-shim-codegen (--target duckdb).
//!
//! Scalars are wired against the `duckdb:extension@2.2.0`
//! contract; aggregates / table functions / pragmas / casts
//! return `Duckerror::Unsupported`. See AGENTS.md (in
//! datalink/crates/datalink-shim-duckdb-emit) for the migration
//! plan that landed this target.

#![allow(unused_imports, dead_code)]

extern crate alloc;

use alloc::format;
use alloc::string::{String, ToString};
use alloc::vec::Vec;

mod bindings {
    wit_bindgen::generate!({
        path: "wit",
        world: "bridge",
        generate_all,
        // Derive serde::Serialize + serde::Deserialize on
        // generated record types so the JSON-direct decode path
        // in the per-record helpers below resolves against the
        // upstream type (`serde_json::from_str::<UPSTREAM>(...)`).
        additional_derives: [serde::Serialize, serde::Deserialize],
        // Contract types + helper-component types + primary-shim
        // variants/flags can't derive serde out-of-the-box.
        additional_derives_ignore: [
            "arrow-error",
            "capability",
            "capabilitykind",
            "cast-kind",
            "cast-spec",
            "columndef",
            "compare-op",
            "complexvalue",
            "configerror",
            "conn-events",
            "copy-from-bind-result",
            "copy-handler",
            "crs-def",
            "decimalvalue",
            "detection-mode",
            "duckerror",
            "duckvalue",
            "extopts",
            "file-info",
            "file-open-result",
            "filter-op",
            "funcarg",
            "funcflags",
            "funcopts",
            "index-hit",
            "intervalvalue",
            "invokeinfo",
            "loadresult",
            "logfield",
            "logical-type",
            "logicaltype",
            "loglevel",
            "macro-def",
            "null-handling",
            "parse-outcome",
            "plan-node",
            "plan-shape",
            "replacement-scan",
            "rewrite-directive",
            "scan-filter",
            "scan-request",
            "secret-kv",
            "secret-param",
            "setting-scope",
            "setting-type",
            "structured-rewrite",
            "table-filter",
            "table-open-result",
            "temporal-error",
            "uuidvalue",
            "window-frame",
        ],
    });
}

use bindings::duckdb::extension::catalog;
use bindings::duckdb::extension::runtime;
use bindings::duckdb::extension::types;
use bindings::exports::duckdb::extension::callback_dispatch;
use bindings::exports::duckdb::extension::guest;

use bindings::mobilitydb::temporal::analytics_ops;
use bindings::mobilitydb::temporal::arithmetic_ops;
use bindings::mobilitydb::temporal::bitemporal_bool_ops;
use bindings::mobilitydb::temporal::bitemporal_float_ops;
use bindings::mobilitydb::temporal::bitemporal_int_ops;
use bindings::mobilitydb::temporal::bitemporal_residue_ops;
use bindings::mobilitydb::temporal::bitemporal_text_ops;
use bindings::mobilitydb::temporal::clustering_ops;
use bindings::mobilitydb::temporal::comparisons_ops;
use bindings::mobilitydb::temporal::join_ops;
use bindings::mobilitydb::temporal::near_done_ops;
use bindings::mobilitydb::temporal::nearest_join_ops;
use bindings::mobilitydb::temporal::other_indexes_ops;
use bindings::mobilitydb::temporal::restrictions_ops;
use bindings::mobilitydb::temporal::sequence_json_ops;
use bindings::mobilitydb::temporal::serde_ops;
use bindings::mobilitydb::temporal::set_extras_ops;
use bindings::mobilitydb::temporal::sets_extended_ops;
use bindings::mobilitydb::temporal::sets_ops;
use bindings::mobilitydb::temporal::similarity_extended_ops;
use bindings::mobilitydb::temporal::similarity_ops;
use bindings::mobilitydb::temporal::span_extras_ops;
use bindings::mobilitydb::temporal::spans_ops;
use bindings::mobilitydb::temporal::spansets_ops;
use bindings::mobilitydb::temporal::spatial_index_ops;
use bindings::mobilitydb::temporal::statistics_ops;
use bindings::mobilitydb::temporal::stbox_ops;
use bindings::mobilitydb::temporal::stindex_ops;
use bindings::mobilitydb::temporal::tbool_ops;
use bindings::mobilitydb::temporal::tcbuffer_extended_ops;
use bindings::mobilitydb::temporal::tcbuffer_ops;
use bindings::mobilitydb::temporal::temporal_aggregate_ops;
use bindings::mobilitydb::temporal::temporal_index_ops;
use bindings::mobilitydb::temporal::temporal_relations_ops;
use bindings::mobilitydb::temporal::textset_ops;
use bindings::mobilitydb::temporal::tfloat_math_ops;
use bindings::mobilitydb::temporal::tfloat_ops;
use bindings::mobilitydb::temporal::tgeogpoint_ops;
use bindings::mobilitydb::temporal::tgeompoint3d_ops;
use bindings::mobilitydb::temporal::tgeompoint_detection_ops;
use bindings::mobilitydb::temporal::tgeompoint_extended_ops;
use bindings::mobilitydb::temporal::tgeompoint_ops;
use bindings::mobilitydb::temporal::time_split_ops;
use bindings::mobilitydb::temporal::tint_math_ops;
use bindings::mobilitydb::temporal::tint_ops;
use bindings::mobilitydb::temporal::tnpoint_extended_ops;
use bindings::mobilitydb::temporal::tnpoint_ops;
use bindings::mobilitydb::temporal::to_rows_ops;
use bindings::mobilitydb::temporal::tpose_extended_ops;
use bindings::mobilitydb::temporal::tpose_ops;
use bindings::mobilitydb::temporal::trajectory_restrictions_ops;
use bindings::mobilitydb::temporal::tstzset_ops;
use bindings::mobilitydb::temporal::tstzspan_ops;
use bindings::mobilitydb::temporal::ttext_ops;
use bindings::mobilitydb::temporal::ttext_strings_ops;
use bindings::mobilitydb::temporal::typed_join_ops;
use bindings::mobilitydb::temporal::value_split_ops;

struct MobilitydbBridge;

// ─── Duckvalue arg helpers ───
//
// Mirror of sqlite-emit's `arg_text`/`arg_blob`/`arg_i64` set
// but unpacking from `types::Duckvalue` arms. The DuckDB FROZEN
// set is wider than SQLite's: integer / unsigned / date / etc.
// arms all coerce to a common Rust primitive via the four
// helpers below.

fn dv_text<'a>(
    args: &'a [types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<&'a str, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Text(s)) => Ok(s.as_str()),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be VARCHAR"
        ))),
    }
}

fn dv_blob<'a>(
    args: &'a [types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<&'a [u8], types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Blob(b)) => Ok(b.as_slice()),
        Some(types::Duckvalue::Text(s)) => Ok(s.as_bytes()),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be BLOB"
        ))),
    }
}

fn dv_f64(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<f64, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Float64(v)) => Ok(*v),
        Some(types::Duckvalue::Float32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int64(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint64(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int16(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int8(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint16(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint8(v)) => Ok(*v as f64),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be DOUBLE"
        ))),
    }
}

fn dv_i64(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<i64, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Int64(v)) => Ok(*v),
        Some(types::Duckvalue::Int32(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint64(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint32(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Int16(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Int8(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint16(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint8(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Boolean(b)) => Ok(if *b { 1 } else { 0 }),
        Some(types::Duckvalue::Float64(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Float32(v)) => Ok(*v as i64),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be INTEGER"
        ))),
    }
}

fn dv_bool(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<bool, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Boolean(b)) => Ok(*b),
        Some(types::Duckvalue::Int64(v)) => Ok(*v != 0),
        Some(types::Duckvalue::Int32(v)) => Ok(*v != 0),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be BOOLEAN"
        ))),
    }
}

/// Generic error formatter for fallible upstream WIT calls. The
/// dispatch arm wraps the upstream error via this fn into a
/// String the Duckerror::Invalidargument arm carries.
fn shim_err_string<E: core::fmt::Debug>(e: E) -> String {
    format!("{:?}", e)
}

// ── JSON-as-TEXT primitive `list<X>` param helpers ──
//
// Mirror of sqlite-emit's `parse_json_list_<suffix>` set but
// error-wrapped into `types::Duckerror::Invalidargument`. The SQL
// caller passes a JSON-array literal in the TEXT/VARCHAR arg
// (e.g. `'[1.0, 2.0, 3.0]'`); the helper decodes via serde_json
// into a `Vec<T>` which the dispatch arm passes to the WIT
// function as `&[T]`.

fn parse_json_list_f64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<f64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<f64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of f64 ({e})"
        ))
    })
}

fn parse_json_list_i32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<i32>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<i32>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s32 ({e})"
        ))
    })
}

fn parse_json_list_i64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<i64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<i64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s64 ({e})"
        ))
    })
}

fn parse_json_list_u32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u32>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u32>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u32 ({e})"
        ))
    })
}

fn parse_json_list_u64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u64 ({e})"
        ))
    })
}

fn parse_json_list_u8(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u8>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u8>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u8 ({e})"
        ))
    })
}

fn parse_json_list_bool(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bool>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bool>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bool ({e})"
        ))
    })
}

fn parse_json_list_string(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<String>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<String>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of string ({e})"
        ))
    })
}

// ─── list<tuple<...>> param helpers ───
// One per unique tuple-element signature referenced by a wired
// dispatch arm. SQL passes a JSON-array of arrays as the TEXT/VARCHAR
// arg (e.g. `'[[1, 10], [20, 30]]'`); serde_json parses tuples
// as fixed-length JSON arrays so the upstream
// `Vec<(T1, T2, ...)>` binding round-trips directly.
fn parse_json_list_tuple_i32_i32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<(i32, i32)>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<(i32, i32)>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of [s32, s32] tuples ({e})"
        ))
    })
}

// ─── Per-record wit-value marshaling helpers ───
// Records ride on `Duckvalue::Complex { type_expr, json }`.
// Decode parses `json` straight into UPSTREAM via
// serde_json::from_str (UPSTREAM derives serde derives via
// wit-bindgen's `additional_derives` arg above). Encode
// round-trips UPSTREAM → JSON → Complex carrier.
fn arg_witvalue_time_period(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::TimePeriod, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::TimePeriod>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_time_period(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::TimePeriod>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::TimePeriod>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of time-period ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_time_period(
    upstream: bindings::mobilitydb::temporal::types::TimePeriod,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode time_period wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/time-period".into(),
        json,
    }))
}

fn arg_witvalue_tint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::TintSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::TintSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::TintSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::TintSequence>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tint-sequence ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_tint_sequence(
    upstream: bindings::mobilitydb::temporal::types::TintSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tint_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/tint-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tfloat_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::TfloatSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::TfloatSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tfloat_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::TfloatSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::TfloatSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tfloat-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_tfloat_sequence(
    upstream: bindings::mobilitydb::temporal::types::TfloatSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tfloat_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/tfloat-sequence".into(),
        json,
    }))
}

fn arg_witvalue_geom_point(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::GeomPoint, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::GeomPoint>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_geom_point(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::GeomPoint>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::GeomPoint>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of geom-point ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_geom_point(
    upstream: bindings::mobilitydb::temporal::types::GeomPoint,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode geom_point wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/geom-point".into(),
        json,
    }))
}

fn arg_witvalue_tgeompoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::TgeompointSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::TgeompointSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tgeompoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::TgeompointSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::TgeompointSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tgeompoint-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_tgeompoint_sequence(
    upstream: bindings::mobilitydb::temporal::types::TgeompointSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tgeompoint_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/tgeompoint-sequence".into(),
        json,
    }))
}

fn arg_witvalue_bounding_box(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::types::BoundingBox, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::types::BoundingBox>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_bounding_box(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::types::BoundingBox>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::types::BoundingBox>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of bounding-box ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_bounding_box(
    upstream: bindings::mobilitydb::temporal::types::BoundingBox,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode bounding_box wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/types/bounding-box".into(),
        json,
    }))
}

fn arg_witvalue_speed_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeompoint_ops::SpeedStats, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeompoint_ops::SpeedStats>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_speed_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::SpeedStats>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::SpeedStats>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of speed-stats ({e})"
            ))
        })
}

fn ret_to_witvalue_speed_stats(
    upstream: bindings::mobilitydb::temporal::tgeompoint_ops::SpeedStats,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode speed_stats wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeompoint-ops/speed-stats".into(),
        json,
    }))
}

fn arg_witvalue_movement_summary(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeompoint_ops::MovementSummary, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeompoint_ops::MovementSummary>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_movement_summary(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::MovementSummary>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::MovementSummary>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of movement-summary ({e})"
        ))
    })
}

fn ret_to_witvalue_movement_summary(
    upstream: bindings::mobilitydb::temporal::tgeompoint_ops::MovementSummary,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode movement_summary wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeompoint-ops/movement-summary".into(),
        json,
    }))
}

fn arg_witvalue_direction_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeompoint_ops::DirectionStats, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeompoint_ops::DirectionStats>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_direction_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::DirectionStats>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::DirectionStats>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of direction-stats ({e})"
        ))
    })
}

fn ret_to_witvalue_direction_stats(
    upstream: bindings::mobilitydb::temporal::tgeompoint_ops::DirectionStats,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode direction_stats wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeompoint-ops/direction-stats".into(),
        json,
    }))
}

fn arg_witvalue_trajectory_analysis(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeompoint_ops::TrajectoryAnalysis, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeompoint_ops::TrajectoryAnalysis>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_trajectory_analysis(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::TrajectoryAnalysis>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tgeompoint_ops::TrajectoryAnalysis>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of trajectory-analysis ({e})"
        ))
    })
}

fn ret_to_witvalue_trajectory_analysis(
    upstream: bindings::mobilitydb::temporal::tgeompoint_ops::TrajectoryAnalysis,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode trajectory_analysis wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeompoint-ops/trajectory-analysis".into(),
        json,
    }))
}

fn arg_witvalue_stbox(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::stbox_ops::Stbox, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::stbox_ops::Stbox>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_stbox(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::stbox_ops::Stbox>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::stbox_ops::Stbox>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of stbox ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_stbox(
    upstream: bindings::mobilitydb::temporal::stbox_ops::Stbox,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode stbox wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/stbox-ops/stbox".into(),
        json,
    }))
}

fn arg_witvalue_stbox3d(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::stbox_ops::Stbox3d, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::stbox_ops::Stbox3d>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_stbox3d(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::stbox_ops::Stbox3d>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::stbox_ops::Stbox3d>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of stbox3d ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_stbox3d(
    upstream: bindings::mobilitydb::temporal::stbox_ops::Stbox3d,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode stbox3d wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/stbox-ops/stbox3d".into(),
        json,
    }))
}

fn arg_witvalue_tbox(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::stbox_ops::Tbox, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::stbox_ops::Tbox>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tbox(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::stbox_ops::Tbox>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::stbox_ops::Tbox>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tbox ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_tbox(
    upstream: bindings::mobilitydb::temporal::stbox_ops::Tbox,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode tbox wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/stbox-ops/tbox".into(),
        json,
    }))
}

fn arg_witvalue_stindex_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::stindex_ops::StindexEntry, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::stindex_ops::StindexEntry>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_stindex_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::stindex_ops::StindexEntry>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::stindex_ops::StindexEntry>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of stindex-entry ({e})"
            ))
        })
}

fn ret_to_witvalue_stindex_entry(
    upstream: bindings::mobilitydb::temporal::stindex_ops::StindexEntry,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode stindex_entry wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/stindex-ops/stindex-entry".into(),
        json,
    }))
}

fn arg_witvalue_ttext_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::ttext_ops::TtextSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::ttext_ops::TtextSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_ttext_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::ttext_ops::TtextSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::ttext_ops::TtextSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of ttext-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_ttext_sequence(
    upstream: bindings::mobilitydb::temporal::ttext_ops::TtextSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode ttext_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/ttext-ops/ttext-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tbool_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tbool_ops::TboolSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tbool_ops::TboolSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tbool_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tbool_ops::TboolSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tbool_ops::TboolSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tbool-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_tbool_sequence(
    upstream: bindings::mobilitydb::temporal::tbool_ops::TboolSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tbool_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tbool-ops/tbool-sequence".into(),
        json,
    }))
}

fn arg_witvalue_int_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::spans_ops::IntSpan, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::spans_ops::IntSpan>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_int_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::spans_ops::IntSpan>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::spans_ops::IntSpan>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of int-span ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_int_span(
    upstream: bindings::mobilitydb::temporal::spans_ops::IntSpan,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode int_span wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/spans-ops/int-span".into(),
        json,
    }))
}

fn arg_witvalue_float_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::spans_ops::FloatSpan, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::spans_ops::FloatSpan>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_float_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::spans_ops::FloatSpan>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::spans_ops::FloatSpan>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of float-span ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_float_span(
    upstream: bindings::mobilitydb::temporal::spans_ops::FloatSpan,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode float_span wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/spans-ops/float-span".into(),
        json,
    }))
}

fn arg_witvalue_date_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::spans_ops::DateSpan, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::spans_ops::DateSpan>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_date_span(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::spans_ops::DateSpan>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::spans_ops::DateSpan>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of date-span ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_date_span(
    upstream: bindings::mobilitydb::temporal::spans_ops::DateSpan,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode date_span wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/spans-ops/date-span".into(),
        json,
    }))
}

fn arg_witvalue_tgeompoint3d_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeompoint3d_ops::Tgeompoint3dSequence, types::Duckerror>
{
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeompoint3d_ops::Tgeompoint3dSequence>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tgeompoint3d_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<bindings::mobilitydb::temporal::tgeompoint3d_ops::Tgeompoint3dSequence>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<
        Vec<bindings::mobilitydb::temporal::tgeompoint3d_ops::Tgeompoint3dSequence>,
    >(text)
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of tgeompoint3d-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_tgeompoint3d_sequence(
    upstream: bindings::mobilitydb::temporal::tgeompoint3d_ops::Tgeompoint3dSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tgeompoint3d_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeompoint3d-ops/tgeompoint3d-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tpose_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tpose_ops::TposeSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tpose_ops::TposeSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tpose_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tpose_ops::TposeSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tpose_ops::TposeSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tpose-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_tpose_sequence(
    upstream: bindings::mobilitydb::temporal::tpose_ops::TposeSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tpose_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tpose-ops/tpose-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tcbuffer_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tcbuffer_ops::TcbufferSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tcbuffer_ops::TcbufferSequence>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tcbuffer_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tcbuffer_ops::TcbufferSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tcbuffer_ops::TcbufferSequence>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of tcbuffer-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_tcbuffer_sequence(
    upstream: bindings::mobilitydb::temporal::tcbuffer_ops::TcbufferSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tcbuffer_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tcbuffer-ops/tcbuffer-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tstzspan(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tstzspan_ops::Tstzspan, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tstzspan_ops::Tstzspan>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tstzspan(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tstzspan_ops::Tstzspan>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tstzspan_ops::Tstzspan>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tstzspan ({e})"
            ))
        })
}

fn ret_to_witvalue_tstzspan(
    upstream: bindings::mobilitydb::temporal::tstzspan_ops::Tstzspan,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode tstzspan wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tstzspan-ops/tstzspan".into(),
        json,
    }))
}

fn arg_witvalue_npoint(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tnpoint_ops::Npoint, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tnpoint_ops::Npoint>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_npoint(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tnpoint_ops::Npoint>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tnpoint_ops::Npoint>>(text).map_err(
        |e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of npoint ({e})"
            ))
        },
    )
}

fn ret_to_witvalue_npoint(
    upstream: bindings::mobilitydb::temporal::tnpoint_ops::Npoint,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode npoint wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tnpoint-ops/npoint".into(),
        json,
    }))
}

fn arg_witvalue_nsegment(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tnpoint_ops::Nsegment, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tnpoint_ops::Nsegment>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_nsegment(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tnpoint_ops::Nsegment>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tnpoint_ops::Nsegment>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of nsegment ({e})"
            ))
        })
}

fn ret_to_witvalue_nsegment(
    upstream: bindings::mobilitydb::temporal::tnpoint_ops::Nsegment,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode nsegment wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tnpoint-ops/nsegment".into(),
        json,
    }))
}

fn arg_witvalue_tnpoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tnpoint_ops::TnpointSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tnpoint_ops::TnpointSequence>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tnpoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tnpoint_ops::TnpointSequence>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tnpoint_ops::TnpointSequence>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of tnpoint-sequence ({e})"
            ))
        })
}

fn ret_to_witvalue_tnpoint_sequence(
    upstream: bindings::mobilitydb::temporal::tnpoint_ops::TnpointSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tnpoint_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tnpoint-ops/tnpoint-sequence".into(),
        json,
    }))
}

fn arg_witvalue_bitemporal_bool_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    bindings::mobilitydb::temporal::bitemporal_bool_ops::BitemporalBoolSequence,
    types::Duckerror,
> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<
        bindings::mobilitydb::temporal::bitemporal_bool_ops::BitemporalBoolSequence,
    >(&cmplx.json)
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_bitemporal_bool_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<bindings::mobilitydb::temporal::bitemporal_bool_ops::BitemporalBoolSequence>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<
        Vec<bindings::mobilitydb::temporal::bitemporal_bool_ops::BitemporalBoolSequence>,
    >(text)
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bitemporal-bool-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_bitemporal_bool_sequence(
    upstream: bindings::mobilitydb::temporal::bitemporal_bool_ops::BitemporalBoolSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode bitemporal_bool_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/bitemporal-bool-ops/bitemporal-bool-sequence".into(),
        json,
    }))
}

fn arg_witvalue_bitemporal_text_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    bindings::mobilitydb::temporal::bitemporal_text_ops::BitemporalTextSequence,
    types::Duckerror,
> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<
        bindings::mobilitydb::temporal::bitemporal_text_ops::BitemporalTextSequence,
    >(&cmplx.json)
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_bitemporal_text_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<bindings::mobilitydb::temporal::bitemporal_text_ops::BitemporalTextSequence>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<
        Vec<bindings::mobilitydb::temporal::bitemporal_text_ops::BitemporalTextSequence>,
    >(text)
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bitemporal-text-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_bitemporal_text_sequence(
    upstream: bindings::mobilitydb::temporal::bitemporal_text_ops::BitemporalTextSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode bitemporal_text_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/bitemporal-text-ops/bitemporal-text-sequence".into(),
        json,
    }))
}

fn arg_witvalue_bitemporal_float_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    bindings::mobilitydb::temporal::bitemporal_float_ops::BitemporalFloatSequence,
    types::Duckerror,
> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<
        bindings::mobilitydb::temporal::bitemporal_float_ops::BitemporalFloatSequence,
    >(&cmplx.json)
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_bitemporal_float_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<bindings::mobilitydb::temporal::bitemporal_float_ops::BitemporalFloatSequence>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<
        Vec<bindings::mobilitydb::temporal::bitemporal_float_ops::BitemporalFloatSequence>,
    >(text)
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bitemporal-float-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_bitemporal_float_sequence(
    upstream: bindings::mobilitydb::temporal::bitemporal_float_ops::BitemporalFloatSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode bitemporal_float_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/bitemporal-float-ops/bitemporal-float-sequence"
            .into(),
        json,
    }))
}

fn arg_witvalue_bitemporal_int_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    bindings::mobilitydb::temporal::bitemporal_int_ops::BitemporalIntSequence,
    types::Duckerror,
> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::bitemporal_int_ops::BitemporalIntSequence>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_bitemporal_int_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<bindings::mobilitydb::temporal::bitemporal_int_ops::BitemporalIntSequence>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<
        Vec<bindings::mobilitydb::temporal::bitemporal_int_ops::BitemporalIntSequence>,
    >(text)
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bitemporal-int-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_bitemporal_int_sequence(
    upstream: bindings::mobilitydb::temporal::bitemporal_int_ops::BitemporalIntSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode bitemporal_int_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/bitemporal-int-ops/bitemporal-int-sequence".into(),
        json,
    }))
}

fn arg_witvalue_tgeogpoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::mobilitydb::temporal::tgeogpoint_ops::TgeogpointSequence, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::mobilitydb::temporal::tgeogpoint_ops::TgeogpointSequence>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_tgeogpoint_sequence(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::mobilitydb::temporal::tgeogpoint_ops::TgeogpointSequence>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::mobilitydb::temporal::tgeogpoint_ops::TgeogpointSequence>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of tgeogpoint-sequence ({e})"
        ))
    })
}

fn ret_to_witvalue_tgeogpoint_sequence(
    upstream: bindings::mobilitydb::temporal::tgeogpoint_ops::TgeogpointSequence,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode tgeogpoint_sequence wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "mobilitydb:temporal@0.1.0/tgeogpoint-ops/tgeogpoint-sequence".into(),
        json,
    }))
}

// ─── Handle tables (u32 -> arm-index, one per dispatch family) ───
//
// `register_scalars()` / `register_aggregates()` / `register_tables()`
// allocate one handle per registration and store its arm index in the
// family-specific table. The corresponding `call_*` dispatch arm
// looks up the index and routes to the per-arm match.

fn handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn aggregate_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn table_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

static NEXT_HANDLE: std::sync::atomic::AtomicU32 = std::sync::atomic::AtomicU32::new(1);

impl guest::Guest for MobilitydbBridge {
    fn load() -> Result<types::Loadresult, types::Duckerror> {
        register_scalars()?;
        register_aggregates()?;
        register_tables()?;
        register_casts()?;
        Ok(types::Loadresult {
            name: "mobilitydb".into(),
            version: Some("0.1.0".into()),
            requires: Vec::new().into(),
        })
    }
    fn reconfigure(_keys: Vec<String>) -> Result<bool, types::Duckerror> {
        Ok(false)
    }
    fn shutdown() -> Result<bool, types::Duckerror> {
        Ok(false)
    }
}

impl callback_dispatch::Guest for MobilitydbBridge {
    fn call_scalar_batch(
        handle: u32,
        rows: Vec<Vec<types::Duckvalue>>,
        ctx: types::Invokeinfo,
    ) -> Result<Vec<types::Duckvalue>, types::Duckerror> {
        let base = ctx.rowindex.unwrap_or(0);
        let mut out = Vec::with_capacity(rows.len());
        for (i, args) in rows.into_iter().enumerate() {
            let row_ctx = types::Invokeinfo {
                rowindex: Some(base + i as u64),
                iswindow: ctx.iswindow,
            };
            out.push(<Self as callback_dispatch::Guest>::call_scalar(
                handle, args, row_ctx,
            )?);
        }
        Ok(out)
    }

    fn call_scalar(
        handle: u32,
        args: Vec<types::Duckvalue>,
        _ctx: types::Invokeinfo,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        let arm_idx = handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown scalar handle".into()))?;
        // Default DuckDB null semantics: a NULL arg short-circuits
        // to NULL before the function runs. The base
        // `runtime.scalar-registry.register` API the codegen uses
        // here doesn't take a null-handling arm (that's
        // `runtime-ext.register-scalar-ex`), so DuckDB enforces
        // the propagate path engine-side. We defensively check
        // here too in case the host plumbs a NULL through.
        if args.iter().any(|v| matches!(v, types::Duckvalue::Null)) {
            return Ok(types::Duckvalue::Null);
        }
        match arm_idx {
            0usize => {
                let arg0 = dv_f64(&args, 0, "angular_diff")?;
                let arg1 = dv_f64(&args, 1, "angular_diff")?;
                Ok(types::Duckvalue::Float64(analytics_ops::angular_diff(
                    arg0, arg1,
                )))
            }
            1usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "average_frechet_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "average_frechet_distance")?;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::average_frechet_distance(&arg0, &arg1),
                ))
            }
            2usize => {
                let arg0 = dv_f64(&args, 0, "bearing")?;
                let arg1 = dv_f64(&args, 1, "bearing")?;
                let arg2 = dv_f64(&args, 2, "bearing")?;
                let arg3 = dv_f64(&args, 3, "bearing")?;
                Ok(types::Duckvalue::Float64(analytics_ops::bearing(
                    arg0, arg1, arg2, arg3,
                )))
            }
            3usize => {
                let arg0 =
                    arg_witvalue_bitemporal_bool_sequence(&args, 0, "bitemporal_bool_as_of")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_bool_as_of")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_bool_as_of")?;
                Ok(
                    match bitemporal_bool_ops::bitemporal_bool_as_of(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Boolean(v),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            4usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_count_current",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_bool_ops::bitemporal_bool_count_current(&arg0) as i64,
                ))
            }
            5usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_count_superseded",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_bool_ops::bitemporal_bool_count_superseded(&arg0) as i64,
                ))
            }
            6usize => {
                let arg0 =
                    arg_witvalue_bitemporal_bool_sequence(&args, 0, "bitemporal_bool_current")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_bool_current(&arg0) as i64,
                ))
            }
            7usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_ever_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_bool_ever_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_bool_ever_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_bool_ever_valid_during_count(
                        &arg0, arg1, arg2,
                    ) as i64,
                ))
            }
            8usize => {
                let arg0 = dv_text(&args, 0, "bitemporal_bool_from_ewkt")?;
                match bitemporal_bool_ops::bitemporal_bool_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_bitemporal_bool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            9usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_history_at_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_bool_history_at_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_bool_history_at_count(&arg0, arg1) as i64,
                ))
            }
            10usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(&args, 0, "bitemporal_bool_len")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_bool_ops::bitemporal_bool_len(&arg0) as i64,
                ))
            }
            11usize => {
                let arg0 =
                    arg_witvalue_bitemporal_bool_sequence(&args, 0, "bitemporal_bool_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (bitemporal_bool_ops::bitemporal_bool_to_ewkt(&arg0)).into(),
                ))
            }
            12usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_transaction_extent",
                )?;
                match bitemporal_bool_ops::bitemporal_bool_transaction_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            13usize => {
                let arg0 =
                    arg_witvalue_bitemporal_bool_sequence(&args, 0, "bitemporal_bool_valid_at")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_bool_valid_at")?;
                Ok(
                    match bitemporal_bool_ops::bitemporal_bool_valid_at(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Boolean(v),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            14usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_bool_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_bool_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_bool_valid_during_count(&arg0, arg1, arg2)
                        as i64,
                ))
            }
            15usize => {
                let arg0 = arg_witvalue_bitemporal_bool_sequence(
                    &args,
                    0,
                    "bitemporal_bool_valid_extent",
                )?;
                match bitemporal_bool_ops::bitemporal_bool_valid_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            16usize => {
                let arg0 =
                    arg_witvalue_bitemporal_float_sequence(&args, 0, "bitemporal_float_as_of")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_float_as_of")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_float_as_of")?;
                Ok(
                    match bitemporal_float_ops::bitemporal_float_as_of(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            17usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_count_current",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_float_ops::bitemporal_float_count_current(&arg0) as i64,
                ))
            }
            18usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_count_superseded",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_float_ops::bitemporal_float_count_superseded(&arg0) as i64,
                ))
            }
            19usize => {
                let arg0 =
                    arg_witvalue_bitemporal_float_sequence(&args, 0, "bitemporal_float_current")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_float_current(&arg0) as i64,
                ))
            }
            20usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_ever_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_float_ever_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_float_ever_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_float_ever_valid_during_count(
                        &arg0, arg1, arg2,
                    ) as i64,
                ))
            }
            21usize => {
                let arg0 = dv_text(&args, 0, "bitemporal_float_from_ewkt")?;
                match bitemporal_float_ops::bitemporal_float_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_bitemporal_float_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            22usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_history_at_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_float_history_at_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_float_history_at_count(&arg0, arg1) as i64,
                ))
            }
            23usize => {
                let arg0 =
                    arg_witvalue_bitemporal_float_sequence(&args, 0, "bitemporal_float_len")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_float_ops::bitemporal_float_len(&arg0) as i64,
                ))
            }
            24usize => {
                let arg0 =
                    arg_witvalue_bitemporal_float_sequence(&args, 0, "bitemporal_float_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (bitemporal_float_ops::bitemporal_float_to_ewkt(&arg0)).into(),
                ))
            }
            25usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_transaction_extent",
                )?;
                match bitemporal_float_ops::bitemporal_float_transaction_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            26usize => {
                let arg0 =
                    arg_witvalue_bitemporal_float_sequence(&args, 0, "bitemporal_float_valid_at")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_float_valid_at")?;
                Ok(
                    match bitemporal_float_ops::bitemporal_float_valid_at(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            27usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_float_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_float_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_float_valid_during_count(&arg0, arg1, arg2)
                        as i64,
                ))
            }
            28usize => {
                let arg0 = arg_witvalue_bitemporal_float_sequence(
                    &args,
                    0,
                    "bitemporal_float_valid_extent",
                )?;
                match bitemporal_float_ops::bitemporal_float_valid_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            29usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_as_of")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_int_as_of")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_int_as_of")?;
                Ok(
                    match bitemporal_int_ops::bitemporal_int_as_of(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Int64(v as i64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            30usize => {
                let arg0 =
                    arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_count_current")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_int_ops::bitemporal_int_count_current(&arg0) as i64,
                ))
            }
            31usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(
                    &args,
                    0,
                    "bitemporal_int_count_superseded",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_int_ops::bitemporal_int_count_superseded(&arg0) as i64,
                ))
            }
            32usize => {
                let arg0 =
                    arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_current")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_int_current(&arg0) as i64,
                ))
            }
            33usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(
                    &args,
                    0,
                    "bitemporal_int_ever_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_int_ever_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_int_ever_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_int_ever_valid_during_count(
                        &arg0, arg1, arg2,
                    ) as i64,
                ))
            }
            34usize => {
                let arg0 = dv_text(&args, 0, "bitemporal_int_from_ewkt")?;
                match bitemporal_int_ops::bitemporal_int_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_bitemporal_int_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            35usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(
                    &args,
                    0,
                    "bitemporal_int_history_at_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_int_history_at_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_int_history_at_count(&arg0, arg1) as i64,
                ))
            }
            36usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_len")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_int_ops::bitemporal_int_len(&arg0) as i64,
                ))
            }
            37usize => {
                let arg0 =
                    arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (bitemporal_int_ops::bitemporal_int_to_ewkt(&arg0)).into(),
                ))
            }
            38usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(
                    &args,
                    0,
                    "bitemporal_int_transaction_extent",
                )?;
                match bitemporal_int_ops::bitemporal_int_transaction_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            39usize => {
                let arg0 =
                    arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_valid_at")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_int_valid_at")?;
                Ok(
                    match bitemporal_int_ops::bitemporal_int_valid_at(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Int64(v as i64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            40usize => {
                let arg0 = arg_witvalue_bitemporal_int_sequence(
                    &args,
                    0,
                    "bitemporal_int_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_int_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_int_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_int_valid_during_count(&arg0, arg1, arg2)
                        as i64,
                ))
            }
            41usize => {
                let arg0 =
                    arg_witvalue_bitemporal_int_sequence(&args, 0, "bitemporal_int_valid_extent")?;
                match bitemporal_int_ops::bitemporal_int_valid_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            42usize => {
                let arg0 =
                    arg_witvalue_bitemporal_text_sequence(&args, 0, "bitemporal_text_as_of")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_text_as_of")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_text_as_of")?;
                Ok(
                    match bitemporal_text_ops::bitemporal_text_as_of(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Text(v.into()),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            43usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_count_current",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_text_ops::bitemporal_text_count_current(&arg0) as i64,
                ))
            }
            44usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_count_superseded",
                )?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_text_ops::bitemporal_text_count_superseded(&arg0) as i64,
                ))
            }
            45usize => {
                let arg0 =
                    arg_witvalue_bitemporal_text_sequence(&args, 0, "bitemporal_text_current")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_text_current(&arg0) as i64,
                ))
            }
            46usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_ever_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_text_ever_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_text_ever_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_text_ever_valid_during_count(
                        &arg0, arg1, arg2,
                    ) as i64,
                ))
            }
            47usize => {
                let arg0 = dv_text(&args, 0, "bitemporal_text_from_ewkt")?;
                match bitemporal_text_ops::bitemporal_text_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_bitemporal_text_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            48usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_history_at_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_text_history_at_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_text_history_at_count(&arg0, arg1) as i64,
                ))
            }
            49usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(&args, 0, "bitemporal_text_len")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_text_ops::bitemporal_text_len(&arg0) as i64,
                ))
            }
            50usize => {
                let arg0 =
                    arg_witvalue_bitemporal_text_sequence(&args, 0, "bitemporal_text_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (bitemporal_text_ops::bitemporal_text_to_ewkt(&arg0)).into(),
                ))
            }
            51usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_transaction_extent",
                )?;
                match bitemporal_text_ops::bitemporal_text_transaction_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            52usize => {
                let arg0 =
                    arg_witvalue_bitemporal_text_sequence(&args, 0, "bitemporal_text_valid_at")?;
                let arg1 = dv_i64(&args, 1, "bitemporal_text_valid_at")?;
                Ok(
                    match bitemporal_text_ops::bitemporal_text_valid_at(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Text(v.into()),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            53usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_valid_during_count",
                )?;
                let arg1 = dv_i64(&args, 1, "bitemporal_text_valid_during_count")?;
                let arg2 = dv_i64(&args, 2, "bitemporal_text_valid_during_count")?;
                Ok(types::Duckvalue::Int64(
                    bitemporal_residue_ops::bitemporal_text_valid_during_count(&arg0, arg1, arg2)
                        as i64,
                ))
            }
            54usize => {
                let arg0 = arg_witvalue_bitemporal_text_sequence(
                    &args,
                    0,
                    "bitemporal_text_valid_extent",
                )?;
                match bitemporal_text_ops::bitemporal_text_valid_extent(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            55usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_adjacent")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "date_span_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_adjacent(
                    arg0, arg1,
                )))
            }
            56usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_contains")?;
                let arg1 = dv_i64(&args, 1, "date_span_contains")? as i32;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_contains(
                    arg0, arg1,
                )))
            }
            57usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_contains_span")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "date_span_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::date_span_contains_span(arg0, arg1),
                ))
            }
            58usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_expand")?;
                let arg1 = dv_i64(&args, 1, "date_span_expand")? as i32;
                match spans_ops::date_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            59usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_intersection")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "date_span_intersection")?;
                match spans_ops::date_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            60usize => {
                let arg0 = dv_i64(&args, 0, "date_span_make")? as i32;
                let arg1 = dv_i64(&args, 1, "date_span_make")? as i32;
                let arg2 = dv_bool(&args, 2, "date_span_make")?;
                let arg3 = dv_bool(&args, 3, "date_span_make")?;
                match spans_ops::date_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            61usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_overlaps")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "date_span_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_overlaps(
                    arg0, arg1,
                )))
            }
            62usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_shift")?;
                let arg1 = dv_i64(&args, 1, "date_span_shift")? as i32;
                ret_to_witvalue_date_span(spans_ops::date_span_shift(arg0, arg1))
            }
            63usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_union")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "date_span_union")?;
                match spans_ops::date_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            64usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "date_span_width")?;
                Ok(types::Duckvalue::Int64(
                    spans_ops::date_span_width(arg0) as i64
                ))
            }
            65usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_contains")?;
                let arg1 = dv_i64(&args, 1, "date_spanset_contains")? as i32;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::date_spanset_contains(&arg0, arg1),
                ))
            }
            66usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_intersection")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "date_spanset_intersection")?;
                {
                    let __r = spansets_ops::date_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            67usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_minus")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "date_spanset_minus")?;
                {
                    let __r = spansets_ops::date_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            68usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::date_spanset_num_spans(&arg0) as i64,
                ))
            }
            69usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_overlaps")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "date_spanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::date_spanset_overlaps(&arg0, &arg1),
                ))
            }
            70usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_union")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "date_spanset_union")?;
                {
                    let __r = spansets_ops::date_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            71usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "date_spanset_width")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::date_spanset_width(&arg0) as i64,
                ))
            }
            72usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_contains")?;
                let arg1 = dv_i64(&args, 1, "dateset_contains")? as i32;
                Ok(types::Duckvalue::Boolean(sets_ops::date_set_contains(
                    &arg0, arg1,
                )))
            }
            73usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_gaps")?;
                {
                    let __r = set_extras_ops::dateset_gaps(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("dateset_gaps: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            74usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_intersection")?;
                let arg1: Vec<i32> = parse_json_list_i32(&args, 1, "dateset_intersection")?;
                {
                    let __r = sets_ops::date_set_intersection(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            75usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_len")?;
                Ok(types::Duckvalue::Int64(sets_ops::date_set_len(&arg0) as i64))
            }
            76usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_make")?;
                {
                    let __r = set_extras_ops::dateset_make(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            77usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_minus")?;
                let arg1: Vec<i32> = parse_json_list_i32(&args, 1, "dateset_minus")?;
                {
                    let __r = sets_ops::date_set_minus(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            78usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_nth")?;
                let arg1 = dv_i64(&args, 1, "dateset_nth")? as u32;
                Ok(match set_extras_ops::dateset_nth(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            79usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_shift")?;
                let arg1 = dv_i64(&args, 1, "dateset_shift")? as i32;
                {
                    let __r = set_extras_ops::dateset_shift(&arg0, arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            80usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_successor")?;
                {
                    let __r = near_done_ops::dateset_successor(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            81usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_to_span")?;
                {
                    match set_extras_ops::dateset_to_span(&arg0) {
                        Some(__t) => {
                            let __json = serde_json::to_string(&__t).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "dateset_to_span: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            82usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (sets_ops::date_set_to_text(&arg0)).into(),
                ))
            }
            83usize => {
                let arg0: Vec<i32> = parse_json_list_i32(&args, 0, "dateset_union")?;
                let arg1: Vec<i32> = parse_json_list_i32(&args, 1, "dateset_union")?;
                {
                    let __r = sets_ops::date_set_union(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            84usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_adjacent")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "datespan_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_adjacent(
                    arg0, arg1,
                )))
            }
            85usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_contains")?;
                let arg1 = dv_i64(&args, 1, "datespan_contains")? as i32;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_contains(
                    arg0, arg1,
                )))
            }
            86usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_contains_span")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "datespan_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::date_span_contains_span(arg0, arg1),
                ))
            }
            87usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_expand")?;
                let arg1 = dv_i64(&args, 1, "datespan_expand")? as i32;
                match spans_ops::date_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            88usize => {
                let arg0 = dv_text(&args, 0, "datespan_from_text")?;
                match spans_ops::date_span_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            89usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_intersection")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "datespan_intersection")?;
                match spans_ops::date_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            90usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_lower")?;
                Ok(types::Duckvalue::Int64(
                    span_extras_ops::datespan_lower(arg0) as i64,
                ))
            }
            91usize => {
                let arg0 = dv_i64(&args, 0, "datespan_make")? as i32;
                let arg1 = dv_i64(&args, 1, "datespan_make")? as i32;
                let arg2 = dv_bool(&args, 2, "datespan_make")?;
                let arg3 = dv_bool(&args, 3, "datespan_make")?;
                match spans_ops::date_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            92usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_overlaps")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "datespan_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::date_span_overlaps(
                    arg0, arg1,
                )))
            }
            93usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_shift")?;
                let arg1 = dv_i64(&args, 1, "datespan_shift")? as i32;
                ret_to_witvalue_date_span(spans_ops::date_span_shift(arg0, arg1))
            }
            94usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::date_span_to_text(arg0)).into(),
                ))
            }
            95usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_union")?;
                let arg1 = arg_witvalue_date_span(&args, 1, "datespan_union")?;
                match spans_ops::date_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_date_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            96usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_upper")?;
                Ok(types::Duckvalue::Int64(
                    span_extras_ops::datespan_upper(arg0) as i64,
                ))
            }
            97usize => {
                let arg0 = arg_witvalue_date_span(&args, 0, "datespan_width")?;
                Ok(types::Duckvalue::Int64(
                    spans_ops::date_span_width(arg0) as i64
                ))
            }
            98usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_contains")?;
                let arg1 = dv_i64(&args, 1, "datespanset_contains")? as i32;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::date_spanset_contains(&arg0, arg1),
                ))
            }
            99usize => {
                let arg0 = dv_text(&args, 0, "datespanset_from_text")?;
                {
                    match spans_ops::date_spanset_from_text(arg0) {
                        Some(__v) => {
                            let __json = serde_json::to_string(&__v).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "datespanset_from_text: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            100usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_intersection")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "datespanset_intersection")?;
                {
                    let __r = spansets_ops::date_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            101usize => {
                let arg0: Vec<(i32, i32)> =
                    parse_json_list_tuple_i32_i32(&args, 0, "datespanset_lower")?;
                Ok(match near_done_ops::datespanset_lower(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            102usize => {
                let arg0: Vec<(i32, i32)> =
                    parse_json_list_tuple_i32_i32(&args, 0, "datespanset_make")?;
                {
                    let __r = near_done_ops::datespanset_make(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("datespanset_make: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            103usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_minus")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "datespanset_minus")?;
                {
                    let __r = spansets_ops::date_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            104usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::date_spanset_num_spans(&arg0) as i64,
                ))
            }
            105usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_overlaps")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "datespanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::date_spanset_overlaps(&arg0, &arg1),
                ))
            }
            106usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::date_spanset_to_text(&arg0)).into(),
                ))
            }
            107usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_union")?;
                let arg1 = parse_json_list_record_date_span(&args, 1, "datespanset_union")?;
                {
                    let __r = spansets_ops::date_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_date_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            108usize => {
                let arg0: Vec<(i32, i32)> =
                    parse_json_list_tuple_i32_i32(&args, 0, "datespanset_upper")?;
                Ok(match near_done_ops::datespanset_upper(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            109usize => {
                let arg0 = parse_json_list_record_date_span(&args, 0, "datespanset_width")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::date_spanset_width(&arg0) as i64,
                ))
            }
            110usize => {
                let arg0 = dv_f64(&args, 0, "distance")?;
                let arg1 = dv_f64(&args, 1, "distance")?;
                let arg2 = dv_f64(&args, 2, "distance")?;
                let arg3 = dv_f64(&args, 3, "distance")?;
                Ok(types::Duckvalue::Float64(analytics_ops::distance(
                    arg0, arg1, arg2, arg3,
                )))
            }
            111usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "dtw_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "dtw_distance")?;
                Ok(types::Duckvalue::Float64(similarity_ops::dtw_distance(
                    &arg0, &arg1,
                )))
            }
            112usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "dtw_distance_normalized")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "dtw_distance_normalized")?;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::dtw_distance_normalized(&arg0, &arg1),
                ))
            }
            113usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "edr_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "edr_distance")?;
                let arg2 = dv_f64(&args, 2, "edr_distance")?;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::edr_distance(&arg0, &arg1, arg2),
                ))
            }
            114usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_adjacent")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "float_span_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_adjacent(
                    arg0, arg1,
                )))
            }
            115usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_contains")?;
                let arg1 = dv_f64(&args, 1, "float_span_contains")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_contains(
                    arg0, arg1,
                )))
            }
            116usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_contains_span")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "float_span_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::float_span_contains_span(arg0, arg1),
                ))
            }
            117usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_expand")?;
                let arg1 = dv_f64(&args, 1, "float_span_expand")?;
                match spans_ops::float_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            118usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_intersection")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "float_span_intersection")?;
                match spans_ops::float_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            119usize => {
                let arg0 = dv_f64(&args, 0, "float_span_make")?;
                let arg1 = dv_f64(&args, 1, "float_span_make")?;
                let arg2 = dv_bool(&args, 2, "float_span_make")?;
                let arg3 = dv_bool(&args, 3, "float_span_make")?;
                match spans_ops::float_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            120usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_overlaps")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "float_span_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_overlaps(
                    arg0, arg1,
                )))
            }
            121usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_shift")?;
                let arg1 = dv_f64(&args, 1, "float_span_shift")?;
                ret_to_witvalue_float_span(spans_ops::float_span_shift(arg0, arg1))
            }
            122usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_union")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "float_span_union")?;
                match spans_ops::float_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            123usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "float_span_width")?;
                Ok(types::Duckvalue::Float64(spans_ops::float_span_width(arg0)))
            }
            124usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_contains")?;
                let arg1 = dv_f64(&args, 1, "float_spanset_contains")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::float_spanset_contains(&arg0, arg1),
                ))
            }
            125usize => {
                let arg0 =
                    parse_json_list_record_float_span(&args, 0, "float_spanset_intersection")?;
                let arg1 =
                    parse_json_list_record_float_span(&args, 1, "float_spanset_intersection")?;
                {
                    let __r = spansets_ops::float_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            126usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_minus")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "float_spanset_minus")?;
                {
                    let __r = spansets_ops::float_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            127usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::float_spanset_num_spans(&arg0) as i64,
                ))
            }
            128usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_overlaps")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "float_spanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::float_spanset_overlaps(&arg0, &arg1),
                ))
            }
            129usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_union")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "float_spanset_union")?;
                {
                    let __r = spansets_ops::float_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            130usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "float_spanset_width")?;
                Ok(types::Duckvalue::Float64(
                    spansets_ops::float_spanset_width(&arg0),
                ))
            }
            131usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_contains")?;
                let arg1 = dv_f64(&args, 1, "floatset_contains")?;
                Ok(types::Duckvalue::Boolean(sets_ops::float_set_contains(
                    &arg0, arg1,
                )))
            }
            132usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_gaps")?;
                {
                    let __r = near_done_ops::floatset_gaps(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("floatset_gaps: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            133usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_intersection")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "floatset_intersection")?;
                {
                    let __r = sets_extended_ops::floatset_intersection(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            134usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_is_disjoint")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "floatset_is_disjoint")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::floatset_is_disjoint(&arg0, &arg1),
                ))
            }
            135usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_is_subset")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "floatset_is_subset")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::floatset_is_subset(&arg0, &arg1),
                ))
            }
            136usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_len")?;
                Ok(types::Duckvalue::Int64(
                    sets_ops::float_set_len(&arg0) as i64
                ))
            }
            137usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_make")?;
                {
                    let __r = set_extras_ops::floatset_make(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            138usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_minus")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "floatset_minus")?;
                {
                    let __r = sets_extended_ops::floatset_minus(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            139usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_nth")?;
                let arg1 = dv_i64(&args, 1, "floatset_nth")? as u32;
                Ok(match set_extras_ops::floatset_nth(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            140usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_scale")?;
                let arg1 = dv_f64(&args, 1, "floatset_scale")?;
                let arg2 = dv_f64(&args, 2, "floatset_scale")?;
                {
                    let __r = set_extras_ops::floatset_scale(&arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            141usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_shift")?;
                let arg1 = dv_f64(&args, 1, "floatset_shift")?;
                {
                    let __r = set_extras_ops::floatset_shift(&arg0, arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            142usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_to_span")?;
                {
                    match set_extras_ops::floatset_to_span(&arg0) {
                        Some(__t) => {
                            let __json = serde_json::to_string(&__t).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "floatset_to_span: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            143usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (sets_ops::float_set_to_text(&arg0)).into(),
                ))
            }
            144usize => {
                let arg0: Vec<f64> = parse_json_list_f64(&args, 0, "floatset_union")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "floatset_union")?;
                {
                    let __r = sets_extended_ops::floatset_union(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            145usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_adjacent")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_adjacent(
                    arg0, arg1,
                )))
            }
            146usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_bucket_list")?;
                let arg1 = dv_f64(&args, 1, "floatspan_bucket_list")?;
                let arg2 = dv_f64(&args, 2, "floatspan_bucket_list")?;
                {
                    let __r = near_done_ops::floatspan_bucket_list(arg0, arg1, arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "floatspan_bucket_list: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            147usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_contains")?;
                let arg1 = dv_f64(&args, 1, "floatspan_contains")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_contains(
                    arg0, arg1,
                )))
            }
            148usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_contains_span")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::float_span_contains_span(arg0, arg1),
                ))
            }
            149usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_expand")?;
                let arg1 = dv_f64(&args, 1, "floatspan_expand")?;
                match spans_ops::float_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            150usize => {
                let arg0 = dv_text(&args, 0, "floatspan_from_text")?;
                match spans_ops::float_span_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            151usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_intersection")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_intersection")?;
                match spans_ops::float_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            152usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_lower")?;
                Ok(types::Duckvalue::Float64(span_extras_ops::floatspan_lower(
                    arg0,
                )))
            }
            153usize => {
                let arg0 = dv_f64(&args, 0, "floatspan_make")?;
                let arg1 = dv_f64(&args, 1, "floatspan_make")?;
                let arg2 = dv_bool(&args, 2, "floatspan_make")?;
                let arg3 = dv_bool(&args, 3, "floatspan_make")?;
                match spans_ops::float_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            154usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_midpoint")?;
                Ok(types::Duckvalue::Float64(
                    span_extras_ops::floatspan_midpoint(arg0),
                ))
            }
            155usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_minus")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_minus")?;
                match span_extras_ops::floatspan_minus(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            156usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_overlaps")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::float_span_overlaps(
                    arg0, arg1,
                )))
            }
            157usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_scale")?;
                let arg1 = dv_f64(&args, 1, "floatspan_scale")?;
                let arg2 = dv_f64(&args, 2, "floatspan_scale")?;
                match span_extras_ops::floatspan_scale(arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            158usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_shift")?;
                let arg1 = dv_f64(&args, 1, "floatspan_shift")?;
                ret_to_witvalue_float_span(spans_ops::float_span_shift(arg0, arg1))
            }
            159usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::float_span_to_text(arg0)).into(),
                ))
            }
            160usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_union")?;
                let arg1 = arg_witvalue_float_span(&args, 1, "floatspan_union")?;
                match spans_ops::float_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_float_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            161usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_upper")?;
                Ok(types::Duckvalue::Float64(span_extras_ops::floatspan_upper(
                    arg0,
                )))
            }
            162usize => {
                let arg0 = arg_witvalue_float_span(&args, 0, "floatspan_width")?;
                Ok(types::Duckvalue::Float64(spans_ops::float_span_width(arg0)))
            }
            163usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_contains")?;
                let arg1 = dv_f64(&args, 1, "floatspanset_contains")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::float_spanset_contains(&arg0, arg1),
                ))
            }
            164usize => {
                let arg0 = dv_text(&args, 0, "floatspanset_from_text")?;
                {
                    match spans_ops::float_spanset_from_text(arg0) {
                        Some(__v) => {
                            let __json = serde_json::to_string(&__v).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "floatspanset_from_text: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            165usize => {
                let arg0 =
                    parse_json_list_record_float_span(&args, 0, "floatspanset_intersection")?;
                let arg1 =
                    parse_json_list_record_float_span(&args, 1, "floatspanset_intersection")?;
                {
                    let __r = spansets_ops::float_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            166usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_minus")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "floatspanset_minus")?;
                {
                    let __r = spansets_ops::float_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            167usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::float_spanset_num_spans(&arg0) as i64,
                ))
            }
            168usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_overlaps")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "floatspanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::float_spanset_overlaps(&arg0, &arg1),
                ))
            }
            169usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::float_spanset_to_text(&arg0)).into(),
                ))
            }
            170usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_union")?;
                let arg1 = parse_json_list_record_float_span(&args, 1, "floatspanset_union")?;
                {
                    let __r = spansets_ops::float_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_float_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            171usize => {
                let arg0 = parse_json_list_record_float_span(&args, 0, "floatspanset_width")?;
                Ok(types::Duckvalue::Float64(
                    spansets_ops::float_spanset_width(&arg0),
                ))
            }
            172usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "frechet_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "frechet_distance")?;
                Ok(types::Duckvalue::Float64(similarity_ops::frechet_distance(
                    &arg0, &arg1,
                )))
            }
            173usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "hausdorff_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "hausdorff_distance")?;
                Ok(types::Duckvalue::Float64(
                    similarity_ops::hausdorff_distance(&arg0, &arg1),
                ))
            }
            174usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_adjacent")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "int_span_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_adjacent(
                    arg0, arg1,
                )))
            }
            175usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_contains")?;
                let arg1 = dv_i64(&args, 1, "int_span_contains")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_contains(
                    arg0, arg1,
                )))
            }
            176usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_contains_span")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "int_span_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::int_span_contains_span(arg0, arg1),
                ))
            }
            177usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_expand")?;
                let arg1 = dv_i64(&args, 1, "int_span_expand")?;
                match spans_ops::int_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            178usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_intersection")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "int_span_intersection")?;
                match spans_ops::int_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            179usize => {
                let arg0 = dv_i64(&args, 0, "int_span_make")?;
                let arg1 = dv_i64(&args, 1, "int_span_make")?;
                let arg2 = dv_bool(&args, 2, "int_span_make")?;
                let arg3 = dv_bool(&args, 3, "int_span_make")?;
                match spans_ops::int_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            180usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_overlaps")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "int_span_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_overlaps(
                    arg0, arg1,
                )))
            }
            181usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_shift")?;
                let arg1 = dv_i64(&args, 1, "int_span_shift")?;
                ret_to_witvalue_int_span(spans_ops::int_span_shift(arg0, arg1))
            }
            182usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_union")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "int_span_union")?;
                match spans_ops::int_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            183usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "int_span_width")?;
                Ok(types::Duckvalue::Int64(
                    spans_ops::int_span_width(arg0) as i64
                ))
            }
            184usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_contains")?;
                let arg1 = dv_i64(&args, 1, "int_spanset_contains")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::int_spanset_contains(&arg0, arg1),
                ))
            }
            185usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_intersection")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "int_spanset_intersection")?;
                {
                    let __r = spansets_ops::int_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            186usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_minus")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "int_spanset_minus")?;
                {
                    let __r = spansets_ops::int_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            187usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::int_spanset_num_spans(&arg0) as i64,
                ))
            }
            188usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_overlaps")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "int_spanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::int_spanset_overlaps(&arg0, &arg1),
                ))
            }
            189usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_union")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "int_spanset_union")?;
                {
                    let __r = spansets_ops::int_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            190usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "int_spanset_width")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::int_spanset_width(&arg0) as i64,
                ))
            }
            191usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_contains")?;
                let arg1 = dv_i64(&args, 1, "intset_contains")?;
                Ok(types::Duckvalue::Boolean(sets_ops::int_set_contains(
                    &arg0, arg1,
                )))
            }
            192usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_gaps")?;
                {
                    let __r = set_extras_ops::intset_gaps(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("intset_gaps: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            193usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_intersection")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "intset_intersection")?;
                {
                    let __r = sets_extended_ops::intset_intersection(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            194usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_is_disjoint")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "intset_is_disjoint")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::intset_is_disjoint(&arg0, &arg1),
                ))
            }
            195usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_is_subset")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "intset_is_subset")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::intset_is_subset(&arg0, &arg1),
                ))
            }
            196usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_len")?;
                Ok(types::Duckvalue::Int64(sets_ops::int_set_len(&arg0) as i64))
            }
            197usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_make")?;
                {
                    let __r = set_extras_ops::intset_make(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            198usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_minus")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "intset_minus")?;
                {
                    let __r = sets_extended_ops::intset_minus(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            199usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_nth")?;
                let arg1 = dv_i64(&args, 1, "intset_nth")? as u32;
                Ok(match set_extras_ops::intset_nth(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            200usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_scale")?;
                let arg1 = dv_f64(&args, 1, "intset_scale")?;
                let arg2 = dv_i64(&args, 2, "intset_scale")?;
                {
                    let __r = set_extras_ops::intset_scale(&arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            201usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_shift")?;
                let arg1 = dv_i64(&args, 1, "intset_shift")?;
                {
                    let __r = set_extras_ops::intset_shift(&arg0, arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            202usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_to_span")?;
                {
                    match set_extras_ops::intset_to_span(&arg0) {
                        Some(__t) => {
                            let __json = serde_json::to_string(&__t).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "intset_to_span: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            203usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (sets_ops::int_set_to_text(&arg0)).into(),
                ))
            }
            204usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "intset_union")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "intset_union")?;
                {
                    let __r = sets_extended_ops::intset_union(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            205usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_adjacent")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_adjacent")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_adjacent(
                    arg0, arg1,
                )))
            }
            206usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_bucket_list")?;
                let arg1 = dv_i64(&args, 1, "intspan_bucket_list")?;
                let arg2 = dv_i64(&args, 2, "intspan_bucket_list")?;
                {
                    let __r = near_done_ops::intspan_bucket_list(arg0, arg1, arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "intspan_bucket_list: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            207usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_contains")?;
                let arg1 = dv_i64(&args, 1, "intspan_contains")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_contains(
                    arg0, arg1,
                )))
            }
            208usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_contains_span")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    spans_ops::int_span_contains_span(arg0, arg1),
                ))
            }
            209usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_expand")?;
                let arg1 = dv_i64(&args, 1, "intspan_expand")?;
                match spans_ops::int_span_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            210usize => {
                let arg0 = dv_text(&args, 0, "intspan_from_text")?;
                match spans_ops::int_span_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            211usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_intersection")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_intersection")?;
                match spans_ops::int_span_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            212usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_lower")?;
                Ok(types::Duckvalue::Int64(
                    span_extras_ops::intspan_lower(arg0) as i64,
                ))
            }
            213usize => {
                let arg0 = dv_i64(&args, 0, "intspan_make")?;
                let arg1 = dv_i64(&args, 1, "intspan_make")?;
                let arg2 = dv_bool(&args, 2, "intspan_make")?;
                let arg3 = dv_bool(&args, 3, "intspan_make")?;
                match spans_ops::int_span_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            214usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_minus")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_minus")?;
                match span_extras_ops::intspan_minus(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            215usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_overlaps")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_overlaps")?;
                Ok(types::Duckvalue::Boolean(spans_ops::int_span_overlaps(
                    arg0, arg1,
                )))
            }
            216usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_scale")?;
                let arg1 = dv_f64(&args, 1, "intspan_scale")?;
                let arg2 = dv_i64(&args, 2, "intspan_scale")?;
                match span_extras_ops::intspan_scale(arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            217usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_shift")?;
                let arg1 = dv_i64(&args, 1, "intspan_shift")?;
                ret_to_witvalue_int_span(spans_ops::int_span_shift(arg0, arg1))
            }
            218usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::int_span_to_text(arg0)).into(),
                ))
            }
            219usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_union")?;
                let arg1 = arg_witvalue_int_span(&args, 1, "intspan_union")?;
                match spans_ops::int_span_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            220usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_upper")?;
                Ok(types::Duckvalue::Int64(
                    span_extras_ops::intspan_upper(arg0) as i64,
                ))
            }
            221usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "intspan_width")?;
                Ok(types::Duckvalue::Int64(
                    spans_ops::int_span_width(arg0) as i64
                ))
            }
            222usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_contains")?;
                let arg1 = dv_i64(&args, 1, "intspanset_contains")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::int_spanset_contains(&arg0, arg1),
                ))
            }
            223usize => {
                let arg0 = dv_text(&args, 0, "intspanset_from_text")?;
                {
                    match spans_ops::int_spanset_from_text(arg0) {
                        Some(__v) => {
                            let __json = serde_json::to_string(&__v).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "intspanset_from_text: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            224usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_intersection")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "intspanset_intersection")?;
                {
                    let __r = spansets_ops::int_spanset_intersection(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            225usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_minus")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "intspanset_minus")?;
                {
                    let __r = spansets_ops::int_spanset_minus(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            226usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_num_spans")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::int_spanset_num_spans(&arg0) as i64,
                ))
            }
            227usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_overlaps")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "intspanset_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    spansets_ops::int_spanset_overlaps(&arg0, &arg1),
                ))
            }
            228usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::int_spanset_to_text(&arg0)).into(),
                ))
            }
            229usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_union")?;
                let arg1 = parse_json_list_record_int_span(&args, 1, "intspanset_union")?;
                {
                    let __r = spansets_ops::int_spanset_union(&arg0, &arg1);
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_int_span(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            230usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "intspanset_width")?;
                Ok(types::Duckvalue::Int64(
                    spansets_ops::int_spanset_width(&arg0) as i64,
                ))
            }
            231usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "lcss_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "lcss_distance")?;
                let arg2 = dv_f64(&args, 2, "lcss_distance")?;
                let arg3 = dv_i64(&args, 3, "lcss_distance")? as u32;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::lcss_distance(&arg0, &arg1, arg2, arg3),
                ))
            }
            232usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "lcss_similarity")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "lcss_similarity")?;
                let arg2 = dv_f64(&args, 2, "lcss_similarity")?;
                let arg3 = dv_i64(&args, 3, "lcss_similarity")? as u32;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::lcss_similarity(&arg0, &arg1, arg2, arg3),
                ))
            }
            233usize => {
                let arg0 = dv_text(&args, 0, "network_distance_via_edges")?;
                let arg1 = dv_i64(&args, 1, "network_distance_via_edges")?;
                let arg2 = dv_i64(&args, 2, "network_distance_via_edges")?;
                Ok(
                    match tgeompoint3d_ops::network_distance_via_edges(arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            234usize => {
                let arg0 = dv_blob(&args, 0, "network_distance_via_graph")?;
                let arg1 = dv_i64(&args, 1, "network_distance_via_graph")?;
                let arg2 = dv_i64(&args, 2, "network_distance_via_graph")?;
                Ok(
                    match tgeompoint3d_ops::network_distance_via_graph(arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            235usize => {
                let arg0 = dv_text(&args, 0, "network_graph_build_from_edges")?;
                Ok(types::Duckvalue::Blob(
                    (tgeompoint3d_ops::network_graph_build_from_edges(arg0)).into(),
                ))
            }
            236usize => {
                let arg0 = dv_text(&args, 0, "network_shortest_path_via_edges")?;
                let arg1 = dv_i64(&args, 1, "network_shortest_path_via_edges")?;
                let arg2 = dv_i64(&args, 2, "network_shortest_path_via_edges")?;
                {
                    let __r = tgeompoint3d_ops::network_shortest_path_via_edges(arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            237usize => {
                let arg0 = dv_blob(&args, 0, "network_shortest_path_via_graph")?;
                let arg1 = dv_i64(&args, 1, "network_shortest_path_via_graph")?;
                let arg2 = dv_i64(&args, 2, "network_shortest_path_via_graph")?;
                {
                    let __r = tgeompoint3d_ops::network_shortest_path_via_graph(arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            238usize => {
                let arg0 = arg_witvalue_npoint(&args, 0, "npoint_distance")?;
                let arg1 = arg_witvalue_npoint(&args, 1, "npoint_distance")?;
                Ok(match tnpoint_ops::npoint_distance(arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            239usize => {
                let arg0 = dv_i64(&args, 0, "npoint_new")?;
                let arg1 = dv_f64(&args, 1, "npoint_new")?;
                ret_to_witvalue_npoint(tnpoint_ops::npoint_new(arg0, arg1))
            }
            240usize => {
                let arg0 = arg_witvalue_npoint(&args, 0, "npoint_same_route")?;
                let arg1 = arg_witvalue_npoint(&args, 1, "npoint_same_route")?;
                Ok(types::Duckvalue::Boolean(tnpoint_ops::npoint_same_route(
                    arg0, arg1,
                )))
            }
            241usize => {
                let arg0 = arg_witvalue_nsegment(&args, 0, "nsegment_contains")?;
                let arg1 = arg_witvalue_npoint(&args, 1, "nsegment_contains")?;
                Ok(types::Duckvalue::Boolean(tnpoint_ops::nsegment_contains(
                    arg0, arg1,
                )))
            }
            242usize => {
                let arg0 = arg_witvalue_nsegment(&args, 0, "nsegment_length")?;
                Ok(types::Duckvalue::Float64(tnpoint_ops::nsegment_length(
                    arg0,
                )))
            }
            243usize => {
                let arg0 = arg_witvalue_nsegment(&args, 0, "nsegment_midpoint")?;
                ret_to_witvalue_npoint(tnpoint_ops::nsegment_midpoint(arg0))
            }
            244usize => {
                let arg0 = dv_i64(&args, 0, "nsegment_new")?;
                let arg1 = dv_f64(&args, 1, "nsegment_new")?;
                let arg2 = dv_f64(&args, 2, "nsegment_new")?;
                ret_to_witvalue_nsegment(tnpoint_ops::nsegment_new(arg0, arg1, arg2).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "nsegment_new: {}",
                            shim_err_string(e)
                        ))
                    },
                )?)
            }
            245usize => {
                let arg0 = arg_witvalue_nsegment(&args, 0, "nsegment_overlaps")?;
                let arg1 = arg_witvalue_nsegment(&args, 1, "nsegment_overlaps")?;
                Ok(types::Duckvalue::Boolean(tnpoint_ops::nsegment_overlaps(
                    arg0, arg1,
                )))
            }
            246usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_adjacent")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_adjacent")?;
                Ok(types::Duckvalue::Boolean(near_done_ops::stbox_adjacent(
                    arg0, arg1,
                )))
            }
            247usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_area")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_area(arg0)))
            }
            248usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_center_time")?;
                Ok(types::Duckvalue::Int64(
                    stbox_ops::stbox_center_time(arg0) as i64
                ))
            }
            249usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_center_x")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_center_x(arg0)))
            }
            250usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_center_y")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_center_y(arg0)))
            }
            251usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_contained_by")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_contained_by")?;
                Ok(types::Duckvalue::Boolean(
                    near_done_ops::stbox_contained_by(arg0, arg1),
                ))
            }
            252usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_contains")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_contains")?;
                Ok(types::Duckvalue::Boolean(stbox_ops::stbox_contains(
                    arg0, arg1,
                )))
            }
            253usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_contains_point")?;
                let arg1 = dv_f64(&args, 1, "stbox_contains_point")?;
                let arg2 = dv_f64(&args, 2, "stbox_contains_point")?;
                let arg3 = dv_i64(&args, 3, "stbox_contains_point")?;
                Ok(types::Duckvalue::Boolean(stbox_ops::stbox_contains_point(
                    arg0, arg1, arg2, arg3,
                )))
            }
            254usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_duration")?;
                Ok(types::Duckvalue::Int64(
                    stbox_ops::stbox_duration(arg0) as i64
                ))
            }
            255usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_expand_spatial")?;
                let arg1 = dv_f64(&args, 1, "stbox_expand_spatial")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_expand_spatial(arg0, arg1))
            }
            256usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_expand_temporal")?;
                let arg1 = dv_i64(&args, 1, "stbox_expand_temporal")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_expand_temporal(arg0, arg1))
            }
            257usize => {
                let arg0 = dv_f64(&args, 0, "stbox_from_spatial")?;
                let arg1 = dv_f64(&args, 1, "stbox_from_spatial")?;
                let arg2 = dv_f64(&args, 2, "stbox_from_spatial")?;
                let arg3 = dv_f64(&args, 3, "stbox_from_spatial")?;
                match stbox_ops::stbox_from_spatial(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_stbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            258usize => {
                let arg0 = arg_witvalue_time_period(&args, 0, "stbox_from_temporal")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_from_temporal(arg0))
            }
            259usize => {
                let arg0 = dv_text(&args, 0, "stbox_from_text")?;
                match stbox_ops::stbox_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_stbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            260usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_height")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_height(arg0)))
            }
            261usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_intersection")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_intersection")?;
                match stbox_ops::stbox_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_stbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            262usize => {
                let arg0 = dv_f64(&args, 0, "stbox_make")?;
                let arg1 = dv_f64(&args, 1, "stbox_make")?;
                let arg2 = dv_f64(&args, 2, "stbox_make")?;
                let arg3 = dv_f64(&args, 3, "stbox_make")?;
                let arg4 = dv_i64(&args, 4, "stbox_make")?;
                let arg5 = dv_i64(&args, 5, "stbox_make")?;
                match stbox_ops::stbox_make(arg0, arg1, arg2, arg3, arg4, arg5) {
                    Some(__rec) => ret_to_witvalue_stbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            263usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_overlaps")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_overlaps")?;
                Ok(types::Duckvalue::Boolean(stbox_ops::stbox_overlaps(
                    arg0, arg1,
                )))
            }
            264usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_same")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_same")?;
                Ok(types::Duckvalue::Boolean(near_done_ops::stbox_same(
                    arg0, arg1,
                )))
            }
            265usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_scale_spatial")?;
                let arg1 = dv_f64(&args, 1, "stbox_scale_spatial")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_scale_spatial(arg0, arg1))
            }
            266usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_scale_temporal")?;
                let arg1 = dv_f64(&args, 1, "stbox_scale_temporal")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_scale_temporal(arg0, arg1))
            }
            267usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_shift_spatial")?;
                let arg1 = dv_f64(&args, 1, "stbox_shift_spatial")?;
                let arg2 = dv_f64(&args, 2, "stbox_shift_spatial")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_shift_spatial(arg0, arg1, arg2))
            }
            268usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_shift_temporal")?;
                let arg1 = dv_i64(&args, 1, "stbox_shift_temporal")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_shift_temporal(arg0, arg1))
            }
            269usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_spatial_distance")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_spatial_distance")?;
                Ok(types::Duckvalue::Float64(
                    stbox_ops::stbox_spatial_distance(arg0, arg1),
                ))
            }
            270usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_spatially_adjacent")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_spatially_adjacent")?;
                Ok(types::Duckvalue::Boolean(
                    stbox_ops::stbox_spatially_adjacent(arg0, arg1),
                ))
            }
            271usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_temporal_distance")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_temporal_distance")?;
                Ok(types::Duckvalue::Int64(
                    stbox_ops::stbox_temporal_distance(arg0, arg1) as i64,
                ))
            }
            272usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_temporally_adjacent")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_temporally_adjacent")?;
                Ok(types::Duckvalue::Boolean(
                    stbox_ops::stbox_temporally_adjacent(arg0, arg1),
                ))
            }
            273usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_tmax")?;
                Ok(types::Duckvalue::Int64(stbox_ops::stbox_tmax(arg0) as i64))
            }
            274usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_tmin")?;
                Ok(types::Duckvalue::Int64(stbox_ops::stbox_tmin(arg0) as i64))
            }
            275usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_to_text")?;
                Ok(types::Duckvalue::Text(
                    (stbox_ops::stbox_to_text(arg0)).into(),
                ))
            }
            276usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_transform")?;
                let arg1 = dv_f64(&args, 1, "stbox_transform")?;
                let arg2 = dv_f64(&args, 2, "stbox_transform")?;
                let arg3 = dv_i64(&args, 3, "stbox_transform")?;
                ret_to_witvalue_stbox(near_done_ops::stbox_transform(arg0, arg1, arg2, arg3))
            }
            277usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_union")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stbox_union")?;
                ret_to_witvalue_stbox(stbox_ops::stbox_union(arg0, arg1))
            }
            278usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_width")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_width(arg0)))
            }
            279usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_xmax")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_xmax(arg0)))
            }
            280usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_xmin")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_xmin(arg0)))
            }
            281usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_ymax")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_ymax(arg0)))
            }
            282usize => {
                let arg0 = arg_witvalue_stbox(&args, 0, "stbox_ymin")?;
                Ok(types::Duckvalue::Float64(stbox_ops::stbox_ymin(arg0)))
            }
            283usize => {
                let arg0 =
                    parse_json_list_record_stindex_entry(&args, 0, "stindex_count_in_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "stindex_count_in_stbox")?;
                Ok(types::Duckvalue::Int64(
                    stindex_ops::stindex_count_in_stbox(&arg0, arg1) as i64,
                ))
            }
            284usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_always_false")?;
                Ok(types::Duckvalue::Boolean(tbool_ops::tbool_always_false(
                    &arg0,
                )))
            }
            285usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_always_true")?;
                Ok(types::Duckvalue::Boolean(tbool_ops::tbool_always_true(
                    &arg0,
                )))
            }
            286usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_and")?;
                let arg1 = arg_witvalue_tbool_sequence(&args, 1, "tbool_and")?;
                match tbool_ops::tbool_and(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            287usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_at_false")?;
                match tbool_ops::tbool_at_false(&arg0) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            288usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_at_period")?;
                let arg1 = dv_i64(&args, 1, "tbool_at_period")?;
                let arg2 = dv_i64(&args, 2, "tbool_at_period")?;
                match restrictions_ops::tbool_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            289usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_at_true")?;
                match tbool_ops::tbool_at_true(&arg0) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            290usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_at_value")?;
                let arg1 = dv_bool(&args, 1, "tbool_at_value")?;
                match restrictions_ops::tbool_at_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            291usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_count_changes")?;
                Ok(types::Duckvalue::Int64(
                    tbool_ops::tbool_count_changes(&arg0) as i64,
                ))
            }
            292usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_count_false")?;
                Ok(types::Duckvalue::Int64(
                    tbool_ops::tbool_count_false(&arg0) as i64
                ))
            }
            293usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_count_true")?;
                Ok(types::Duckvalue::Int64(
                    tbool_ops::tbool_count_true(&arg0) as i64
                ))
            }
            294usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_duration")?;
                Ok(types::Duckvalue::Int64(
                    tbool_ops::tbool_duration(&arg0) as i64
                ))
            }
            295usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_end_timestamp")?;
                Ok(match tbool_ops::tbool_end_timestamp(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            296usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_end_value")?;
                Ok(match tbool_ops::tbool_end_value(&arg0) {
                    Some(v) => types::Duckvalue::Boolean(v),
                    None => types::Duckvalue::Null,
                })
            }
            297usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_ever_false")?;
                Ok(types::Duckvalue::Boolean(tbool_ops::tbool_ever_false(
                    &arg0,
                )))
            }
            298usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_ever_true")?;
                Ok(types::Duckvalue::Boolean(tbool_ops::tbool_ever_true(&arg0)))
            }
            299usize => {
                let arg0 = dv_text(&args, 0, "tbool_from_csv")?;
                match tbool_ops::tbool_from_csv(arg0) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            300usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_minus_period")?;
                let arg1 = dv_i64(&args, 1, "tbool_minus_period")?;
                let arg2 = dv_i64(&args, 2, "tbool_minus_period")?;
                match restrictions_ops::tbool_minus_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            301usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_minus_value")?;
                let arg1 = dv_bool(&args, 1, "tbool_minus_value")?;
                match restrictions_ops::tbool_minus_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            302usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_not")?;
                ret_to_witvalue_tbool_sequence(tbool_ops::tbool_not(&arg0))
            }
            303usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tbool_ops::tbool_num_instants(&arg0) as i64,
                ))
            }
            304usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_or")?;
                let arg1 = arg_witvalue_tbool_sequence(&args, 1, "tbool_or")?;
                match tbool_ops::tbool_or(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            305usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_start_timestamp")?;
                Ok(match tbool_ops::tbool_start_timestamp(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            306usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_start_value")?;
                Ok(match tbool_ops::tbool_start_value(&arg0) {
                    Some(v) => types::Duckvalue::Boolean(v),
                    None => types::Duckvalue::Null,
                })
            }
            307usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_time_span")?;
                match tbool_ops::tbool_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            308usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_to_csv")?;
                Ok(types::Duckvalue::Text(
                    (tbool_ops::tbool_to_csv(&arg0)).into(),
                ))
            }
            309usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_to_json")?;
                Ok(types::Duckvalue::Text(
                    (tbool_ops::tbool_to_json(&arg0)).into(),
                ))
            }
            310usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "tbool_value_at")?;
                let arg1 = dv_i64(&args, 1, "tbool_value_at")?;
                Ok(match tbool_ops::tbool_value_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Boolean(v),
                    None => types::Duckvalue::Null,
                })
            }
            311usize => {
                let arg0 = dv_text(&args, 0, "tbox_from_text")?;
                match stbox_ops::tbox_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_tbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            312usize => {
                let arg0 = arg_witvalue_tbox(&args, 0, "tbox_to_text")?;
                Ok(types::Duckvalue::Text(
                    (stbox_ops::tbox_to_text(arg0)).into(),
                ))
            }
            313usize => {
                let arg0 =
                    arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_always_contains_point")?;
                let arg1 = dv_f64(&args, 1, "tcbuffer_always_contains_point")?;
                let arg2 = dv_f64(&args, 2, "tcbuffer_always_contains_point")?;
                Ok(types::Duckvalue::Boolean(
                    tcbuffer_extended_ops::tcbuffer_always_contains_point(&arg0, arg1, arg2),
                ))
            }
            314usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_area_at")?;
                let arg1 = dv_i64(&args, 1, "tcbuffer_area_at")?;
                Ok(match tcbuffer_ops::tcbuffer_area_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            315usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_avg_area")?;
                Ok(match tcbuffer_extended_ops::tcbuffer_avg_area(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            316usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_avg_radius")?;
                Ok(match tcbuffer_extended_ops::tcbuffer_avg_radius(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            317usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_center_path_length")?;
                Ok(types::Duckvalue::Float64(
                    tcbuffer_extended_ops::tcbuffer_center_path_length(&arg0),
                ))
            }
            318usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_contains_point_at")?;
                let arg1 = dv_f64(&args, 1, "tcbuffer_contains_point_at")?;
                let arg2 = dv_f64(&args, 2, "tcbuffer_contains_point_at")?;
                let arg3 = dv_i64(&args, 3, "tcbuffer_contains_point_at")?;
                Ok(types::Duckvalue::Boolean(
                    tcbuffer_extended_ops::tcbuffer_contains_point_at(&arg0, arg1, arg2, arg3),
                ))
            }
            319usize => {
                let arg0 =
                    arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_ever_contains_point")?;
                let arg1 = dv_f64(&args, 1, "tcbuffer_ever_contains_point")?;
                let arg2 = dv_f64(&args, 2, "tcbuffer_ever_contains_point")?;
                Ok(types::Duckvalue::Boolean(
                    tcbuffer_extended_ops::tcbuffer_ever_contains_point(&arg0, arg1, arg2),
                ))
            }
            320usize => {
                let arg0 = dv_text(&args, 0, "tcbuffer_from_ewkt")?;
                match tcbuffer_ops::tcbuffer_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_tcbuffer_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            321usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_get_area")?;
                ret_to_witvalue_tfloat_sequence(tcbuffer_extended_ops::tcbuffer_get_area(&arg0))
            }
            322usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_get_center_x")?;
                ret_to_witvalue_tfloat_sequence(tcbuffer_extended_ops::tcbuffer_get_center_x(&arg0))
            }
            323usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_get_center_y")?;
                ret_to_witvalue_tfloat_sequence(tcbuffer_extended_ops::tcbuffer_get_center_y(&arg0))
            }
            324usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_get_radius")?;
                ret_to_witvalue_tfloat_sequence(tcbuffer_extended_ops::tcbuffer_get_radius(&arg0))
            }
            325usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_max_radius")?;
                Ok(match tcbuffer_ops::tcbuffer_max_radius(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            326usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_min_radius")?;
                Ok(match tcbuffer_ops::tcbuffer_min_radius(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            327usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tcbuffer_ops::tcbuffer_num_instants(&arg0) as i64,
                ))
            }
            328usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_radius_at")?;
                let arg1 = dv_i64(&args, 1, "tcbuffer_radius_at")?;
                Ok(match tcbuffer_ops::tcbuffer_radius_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            329usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_swept_area")?;
                Ok(types::Duckvalue::Float64(
                    tcbuffer_extended_ops::tcbuffer_swept_area(&arg0),
                ))
            }
            330usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_time_span")?;
                match tcbuffer_ops::tcbuffer_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            331usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (tcbuffer_ops::tcbuffer_to_ewkt(&arg0)).into(),
                ))
            }
            332usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_to_tgeompoint")?;
                ret_to_witvalue_tgeompoint_sequence(tcbuffer_extended_ops::tcbuffer_to_tgeompoint(
                    &arg0,
                ))
            }
            333usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_total_area")?;
                Ok(types::Duckvalue::Float64(
                    tcbuffer_extended_ops::tcbuffer_total_area(&arg0),
                ))
            }
            334usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_x_at")?;
                let arg1 = dv_i64(&args, 1, "tcbuffer_x_at")?;
                Ok(match tcbuffer_ops::tcbuffer_x_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            335usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_y_at")?;
                let arg1 = dv_i64(&args, 1, "tcbuffer_y_at")?;
                Ok(match tcbuffer_ops::tcbuffer_y_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            336usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_contains")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_contains")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_contains(arg0, arg1),
                ))
            }
            337usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_during")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_during")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_during(arg0, arg1),
                ))
            }
            338usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_finishes")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_finishes")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_finishes(arg0, arg1),
                ))
            }
            339usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_follows")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_follows")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_follows(arg0, arg1),
                ))
            }
            340usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_meets")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_meets")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_meets(arg0, arg1),
                ))
            }
            341usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_overlaps")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_overlaps(arg0, arg1),
                ))
            }
            342usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_precedes")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_precedes")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_precedes(arg0, arg1),
                ))
            }
            343usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "temporal_starts")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "temporal_starts")?;
                Ok(types::Duckvalue::Boolean(
                    temporal_relations_ops::temporal_starts(arg0, arg1),
                ))
            }
            344usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_contains")?;
                let arg1 = dv_text(&args, 1, "textset_contains")?;
                Ok(types::Duckvalue::Boolean(textset_ops::textset_contains(
                    &arg0, arg1,
                )))
            }
            345usize => {
                let arg0 = dv_text(&args, 0, "textset_from_text")?;
                {
                    let __r = textset_ops::textset_from_text(arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            346usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_intersection")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_intersection")?;
                {
                    let __r = sets_extended_ops::textset_intersection(&arg0, &arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            347usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_is_disjoint")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_is_disjoint")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::textset_is_disjoint(&arg0, &arg1),
                ))
            }
            348usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_is_subset")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_is_subset")?;
                Ok(types::Duckvalue::Boolean(
                    sets_extended_ops::textset_is_subset(&arg0, &arg1),
                ))
            }
            349usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_is_superset")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_is_superset")?;
                Ok(types::Duckvalue::Boolean(textset_ops::textset_is_superset(
                    &arg0, &arg1,
                )))
            }
            350usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_len")?;
                Ok(types::Duckvalue::Int64(
                    textset_ops::textset_len(&arg0) as i64
                ))
            }
            351usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_make")?;
                {
                    let __r = textset_ops::textset_make(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            352usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_max")?;
                Ok(match textset_ops::textset_max(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            353usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_min")?;
                Ok(match textset_ops::textset_min(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            354usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_minus")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_minus")?;
                {
                    let __r = sets_extended_ops::textset_minus(&arg0, &arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            355usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_symdiff")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_symdiff")?;
                {
                    let __r = textset_ops::textset_symdiff(&arg0, &arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            356usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (textset_ops::textset_to_text(&arg0)).into(),
                ))
            }
            357usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_union")?;
                let arg1: Vec<String> = parse_json_list_string(&args, 1, "textset_union")?;
                {
                    let __r = sets_extended_ops::textset_union(&arg0, &arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            358usize => {
                let arg0: Vec<String> = parse_json_list_string(&args, 0, "textset_values")?;
                {
                    let __r = textset_ops::textset_values(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            359usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_abs")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_abs(&arg0))
            }
            360usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_add")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_add")?;
                match arithmetic_ops::tfloat_add(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            361usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_add_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_add_scalar")?;
                ret_to_witvalue_tfloat_sequence(arithmetic_ops::tfloat_add_scalar(&arg0, arg1))
            }
            362usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_eq")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_eq")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_eq(&arg0, arg1),
                ))
            }
            363usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_eq_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_eq_scalar")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_eq(&arg0, arg1),
                ))
            }
            364usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_gt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_gt")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_gt(&arg0, arg1),
                ))
            }
            365usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_gt_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_gt_scalar")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_gt(&arg0, arg1),
                ))
            }
            366usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_lt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_lt")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_lt(&arg0, arg1),
                ))
            }
            367usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_always_lt_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_always_lt_scalar")?;
                Ok(types::Duckvalue::Boolean(
                    comparisons_ops::tfloat_always_lt(&arg0, arg1),
                ))
            }
            368usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_at_max")?;
                match restrictions_ops::tfloat_at_max(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            369usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_at_min")?;
                match restrictions_ops::tfloat_at_min(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            370usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_at_period")?;
                let arg1 = dv_i64(&args, 1, "tfloat_at_period")?;
                let arg2 = dv_i64(&args, 2, "tfloat_at_period")?;
                match restrictions_ops::tfloat_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            371usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_at_value")?;
                let arg1 = dv_f64(&args, 1, "tfloat_at_value")?;
                match restrictions_ops::tfloat_at_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            372usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_cos")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_cos(&arg0))
            }
            373usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_derivative")?;
                match statistics_ops::tfloat_derivative(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            374usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_derivative_wit")?;
                match statistics_ops::tfloat_derivative(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            375usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_detect_periodicity")?;
                let arg1 = dv_i64(&args, 1, "tfloat_detect_periodicity")? as u32;
                Ok(match tfloat_ops::tfloat_detect_periodicity(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            376usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_div")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_div")?;
                match arithmetic_ops::tfloat_div(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            377usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_div_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_div_scalar")?;
                match arithmetic_ops::tfloat_div_scalar(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            378usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_eq")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_eq")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_eq(
                    &arg0, arg1,
                )))
            }
            379usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_eq_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_eq_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_eq(
                    &arg0, arg1,
                )))
            }
            380usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_gt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_gt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_gt(
                    &arg0, arg1,
                )))
            }
            381usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_gt_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_gt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_gt(
                    &arg0, arg1,
                )))
            }
            382usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_lt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_lt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_lt(
                    &arg0, arg1,
                )))
            }
            383usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ever_lt_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_ever_lt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tfloat_ever_lt(
                    &arg0, arg1,
                )))
            }
            384usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_exp")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_exp(&arg0))
            }
            385usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_integral")?;
                Ok(types::Duckvalue::Float64(statistics_ops::tfloat_integral(
                    &arg0,
                )))
            }
            386usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_ln")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_ln(&arg0))
            }
            387usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_log10")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_log10(&arg0))
            }
            388usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_max_value")?;
                Ok(match tfloat_ops::tfloat_max_value(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            389usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_max_value_wit")?;
                Ok(match tfloat_ops::tfloat_max_value(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            390usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_min_value")?;
                Ok(match tfloat_ops::tfloat_min_value(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            391usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_min_value_wit")?;
                Ok(match tfloat_ops::tfloat_min_value(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            392usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_minus_period")?;
                let arg1 = dv_i64(&args, 1, "tfloat_minus_period")?;
                let arg2 = dv_i64(&args, 2, "tfloat_minus_period")?;
                match restrictions_ops::tfloat_minus_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            393usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_minus_value")?;
                let arg1 = dv_f64(&args, 1, "tfloat_minus_value")?;
                match restrictions_ops::tfloat_minus_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            394usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_mul")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_mul")?;
                match arithmetic_ops::tfloat_mul(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            395usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_mul_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_mul_scalar")?;
                ret_to_witvalue_tfloat_sequence(arithmetic_ops::tfloat_mul_scalar(&arg0, arg1))
            }
            396usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_negate")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_negate(&arg0))
            }
            397usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tfloat_ops::tfloat_num_instants(&arg0) as i64,
                ))
            }
            398usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_num_instants_wit")?;
                Ok(types::Duckvalue::Int64(
                    tfloat_ops::tfloat_num_instants(&arg0) as i64,
                ))
            }
            399usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_pct_change")?;
                ret_to_witvalue_tfloat_sequence(tfloat_ops::tfloat_pct_change(&arg0))
            }
            400usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_pow")?;
                let arg1 = dv_f64(&args, 1, "tfloat_pow")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_pow(&arg0, arg1))
            }
            401usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_sample_entropy")?;
                let arg1 = dv_i64(&args, 1, "tfloat_sample_entropy")? as u32;
                let arg2 = dv_f64(&args, 2, "tfloat_sample_entropy")?;
                Ok(
                    match statistics_ops::tfloat_sample_entropy(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            402usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_sin")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_sin(&arg0))
            }
            403usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_sqrt")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_sqrt(&arg0))
            }
            404usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_sub")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_sub")?;
                match arithmetic_ops::tfloat_sub(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            405usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_sub_scalar")?;
                let arg1 = dv_f64(&args, 1, "tfloat_sub_scalar")?;
                ret_to_witvalue_tfloat_sequence(arithmetic_ops::tfloat_sub_scalar(&arg0, arg1))
            }
            406usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_tan")?;
                ret_to_witvalue_tfloat_sequence(tfloat_math_ops::tfloat_tan(&arg0))
            }
            407usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_correlation")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_correlation")?;
                let arg2 = dv_i64(&args, 2, "tfloat_temporal_correlation")? as u32;
                match statistics_ops::tfloat_temporal_correlation(&arg0, &arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            408usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_covariance")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_covariance")?;
                let arg2 = dv_i64(&args, 2, "tfloat_temporal_covariance")? as u32;
                match statistics_ops::tfloat_temporal_covariance(&arg0, &arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            409usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_eq")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_eq")?;
                match comparisons_ops::tfloat_temporal_eq(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            410usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_ge")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_ge")?;
                match comparisons_ops::tfloat_temporal_ge(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            411usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_gt")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_gt")?;
                match comparisons_ops::tfloat_temporal_gt(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            412usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_le")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_le")?;
                match comparisons_ops::tfloat_temporal_le(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            413usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_lt")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_lt")?;
                match comparisons_ops::tfloat_temporal_lt(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            414usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_temporal_ne")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "tfloat_temporal_ne")?;
                match comparisons_ops::tfloat_temporal_ne(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            415usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_time_span")?;
                match tfloat_ops::tfloat_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            416usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_to_json")?;
                Ok(types::Duckvalue::Text(
                    (serde_ops::tfloat_to_json(&arg0)).into(),
                ))
            }
            417usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twavg")?;
                Ok(match tfloat_math_ops::tfloat_twavg(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            418usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twcount")?;
                Ok(types::Duckvalue::Float64(tfloat_ops::tfloat_twcount(&arg0)))
            }
            419usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twmax")?;
                Ok(match tfloat_math_ops::tfloat_twmax(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            420usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twmin")?;
                Ok(match tfloat_math_ops::tfloat_twmin(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            421usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twstddev")?;
                Ok(match tfloat_ops::tfloat_twstddev(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            422usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twsum")?;
                Ok(types::Duckvalue::Float64(tfloat_math_ops::tfloat_twsum(
                    &arg0,
                )))
            }
            423usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_twvariance")?;
                Ok(match tfloat_ops::tfloat_twvariance(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            424usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_value_at")?;
                let arg1 = dv_i64(&args, 1, "tfloat_value_at")?;
                Ok(match tfloat_ops::tfloat_value_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            425usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_eq")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_eq")?;
                match restrictions_ops::tfloat_when_eq(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            426usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_ge")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_ge")?;
                match restrictions_ops::tfloat_when_ge(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            427usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_gt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_gt")?;
                match restrictions_ops::tfloat_when_gt(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            428usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_le")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_le")?;
                match restrictions_ops::tfloat_when_le(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            429usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_lt")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_lt")?;
                match restrictions_ops::tfloat_when_lt(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            430usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_when_ne")?;
                let arg1 = dv_f64(&args, 1, "tfloat_when_ne")?;
                match restrictions_ops::tfloat_when_ne(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            431usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_always_dwithin_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_always_dwithin_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_always_dwithin_point_geodetic")?;
                let arg3 = dv_f64(&args, 3, "tgeogpoint_always_dwithin_point_geodetic")?;
                Ok(types::Duckvalue::Boolean(
                    tgeogpoint_ops::tgeogpoint_always_dwithin_point_geodetic(
                        &arg0, arg1, arg2, arg3,
                    ),
                ))
            }
            432usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_at_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "tgeogpoint_at_stbox")?;
                match trajectory_restrictions_ops::tgeogpoint_at_stbox(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tgeogpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            433usize => {
                let arg0 =
                    arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_avg_speed_geodetic")?;
                Ok(match tgeogpoint_ops::tgeogpoint_avg_speed_geodetic(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            434usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_azimuth_at")?;
                let arg1 = dv_i64(&args, 1, "tgeogpoint_azimuth_at")?;
                Ok(match tgeogpoint_ops::tgeogpoint_azimuth_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            435usize => {
                let arg0 =
                    arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_distance_to_point")?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_distance_to_point")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_distance_to_point")?;
                Ok(
                    match tgeogpoint_ops::tgeogpoint_distance_to_point(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            436usize => {
                let arg0 =
                    arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_distance_to_point_seq")?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_distance_to_point_seq")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_distance_to_point_seq")?;
                match tgeogpoint_ops::tgeogpoint_distance_to_point_seq(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            437usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_dwithin_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_dwithin_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_dwithin_point_geodetic")?;
                let arg3 = dv_f64(&args, 3, "tgeogpoint_dwithin_point_geodetic")?;
                Ok(types::Duckvalue::Boolean(
                    tgeogpoint_ops::tgeogpoint_dwithin_point_geodetic(&arg0, arg1, arg2, arg3),
                ))
            }
            438usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_lat_at")?;
                let arg1 = dv_i64(&args, 1, "tgeogpoint_lat_at")?;
                Ok(match tgeogpoint_ops::tgeogpoint_lat_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            439usize => {
                let arg0 =
                    arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_length_geodetic")?;
                Ok(types::Duckvalue::Float64(
                    tgeogpoint_ops::tgeogpoint_length_geodetic(&arg0),
                ))
            }
            440usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_lon_at")?;
                let arg1 = dv_i64(&args, 1, "tgeogpoint_lon_at")?;
                Ok(match tgeogpoint_ops::tgeogpoint_lon_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            441usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_max_distance_to_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_max_distance_to_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_max_distance_to_point_geodetic")?;
                Ok(
                    match tgeogpoint_ops::tgeogpoint_max_distance_to_point_geodetic(
                        &arg0, arg1, arg2,
                    ) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            442usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_min_distance")?;
                let arg1 = arg_witvalue_tgeogpoint_sequence(&args, 1, "tgeogpoint_min_distance")?;
                Ok(
                    match tgeogpoint_ops::tgeogpoint_min_distance(&arg0, &arg1) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            443usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_min_distance_to_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_min_distance_to_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_min_distance_to_point_geodetic")?;
                Ok(
                    match tgeogpoint_ops::tgeogpoint_min_distance_to_point_geodetic(
                        &arg0, arg1, arg2,
                    ) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            444usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_minus_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "tgeogpoint_minus_stbox")?;
                match trajectory_restrictions_ops::tgeogpoint_minus_stbox(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tgeogpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            445usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tgeogpoint_ops::tgeogpoint_num_instants(&arg0) as i64,
                ))
            }
            446usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_num_stops")?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_num_stops")?;
                let arg2 = dv_i64(&args, 2, "tgeogpoint_num_stops")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeogpoint_num_stops(&arg0, arg1, arg2) as i64,
                ))
            }
            447usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_num_turns")?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_num_turns")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeogpoint_num_turns(&arg0, arg1) as i64,
                ))
            }
            448usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_speed_at")?;
                let arg1 = dv_i64(&args, 1, "tgeogpoint_speed_at")?;
                Ok(match tgeogpoint_ops::tgeogpoint_speed_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            449usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_tdisjoint_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_tdisjoint_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_tdisjoint_point_geodetic")?;
                let arg3 = dv_f64(&args, 3, "tgeogpoint_tdisjoint_point_geodetic")?;
                Ok(types::Duckvalue::Boolean(
                    tgeogpoint_ops::tgeogpoint_tdisjoint_point_geodetic(&arg0, arg1, arg2, arg3),
                ))
            }
            450usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_time_span")?;
                match tgeogpoint_ops::tgeogpoint_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            451usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(
                    &args,
                    0,
                    "tgeogpoint_tintersects_point_geodetic",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeogpoint_tintersects_point_geodetic")?;
                let arg2 = dv_f64(&args, 2, "tgeogpoint_tintersects_point_geodetic")?;
                let arg3 = dv_f64(&args, 3, "tgeogpoint_tintersects_point_geodetic")?;
                Ok(types::Duckvalue::Boolean(
                    tgeogpoint_ops::tgeogpoint_tintersects_point_geodetic(&arg0, arg1, arg2, arg3),
                ))
            }
            452usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_at_point")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint3d_at_point")?;
                let arg2 = dv_f64(&args, 2, "tgeompoint3d_at_point")?;
                let arg3 = dv_f64(&args, 3, "tgeompoint3d_at_point")?;
                let arg4 = dv_f64(&args, 4, "tgeompoint3d_at_point")?;
                match trajectory_restrictions_ops::tgeompoint3d_at_point(
                    &arg0, arg1, arg2, arg3, arg4,
                ) {
                    Some(__rec) => ret_to_witvalue_tgeompoint3d_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            453usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_avg_speed")?;
                Ok(match tgeompoint3d_ops::tgeompoint3d_avg_speed(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            454usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_azimuth")?;
                match tgeompoint3d_ops::tgeompoint3d_azimuth(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            455usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_cumulative_length")?;
                match tgeompoint3d_ops::tgeompoint3d_cumulative_length(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            456usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_distance_to_point")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint3d_distance_to_point")?;
                let arg2 = dv_f64(&args, 2, "tgeompoint3d_distance_to_point")?;
                let arg3 = dv_f64(&args, 3, "tgeompoint3d_distance_to_point")?;
                let arg4 = dv_i64(&args, 4, "tgeompoint3d_distance_to_point")?;
                Ok(
                    match tgeompoint3d_ops::tgeompoint3d_distance_to_point(
                        &arg0, arg1, arg2, arg3, arg4,
                    ) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            457usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_dtw_distance")?;
                let arg1 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 1, "tgeompoint3d_dtw_distance")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_dtw_distance(&arg0, &arg1),
                ))
            }
            458usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_frechet_distance")?;
                let arg1 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 1, "tgeompoint3d_frechet_distance")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_frechet_distance(&arg0, &arg1),
                ))
            }
            459usize => {
                let arg0 = dv_text(&args, 0, "tgeompoint3d_from_csv")?;
                match tgeompoint3d_ops::tgeompoint3d_from_csv(arg0) {
                    Some(__rec) => ret_to_witvalue_tgeompoint3d_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            460usize => {
                let arg0 = dv_text(&args, 0, "tgeompoint3d_from_ewkt")?;
                match tgeompoint3d_ops::tgeompoint3d_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_tgeompoint3d_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            461usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(
                    &args,
                    0,
                    "tgeompoint3d_hausdorff_distance",
                )?;
                let arg1 = arg_witvalue_tgeompoint3d_sequence(
                    &args,
                    1,
                    "tgeompoint3d_hausdorff_distance",
                )?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_hausdorff_distance(&arg0, &arg1),
                ))
            }
            462usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_length")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_length(&arg0),
                ))
            }
            463usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_minus_point")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint3d_minus_point")?;
                let arg2 = dv_f64(&args, 2, "tgeompoint3d_minus_point")?;
                let arg3 = dv_f64(&args, 3, "tgeompoint3d_minus_point")?;
                let arg4 = dv_f64(&args, 4, "tgeompoint3d_minus_point")?;
                match trajectory_restrictions_ops::tgeompoint3d_minus_point(
                    &arg0, arg1, arg2, arg3, arg4,
                ) {
                    Some(__rec) => ret_to_witvalue_tgeompoint3d_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            464usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint3d_ops::tgeompoint3d_num_instants(&arg0) as i64,
                ))
            }
            465usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_pitch")?;
                match tgeompoint3d_ops::tgeompoint3d_pitch(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            466usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_planar_length")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_planar_length(&arg0),
                ))
            }
            467usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_time_span")?;
                match tgeompoint3d_ops::tgeompoint3d_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            468usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_to_csv")?;
                Ok(types::Duckvalue::Text(
                    (tgeompoint3d_ops::tgeompoint3d_to_csv(&arg0)).into(),
                ))
            }
            469usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (tgeompoint3d_ops::tgeompoint3d_to_ewkt(&arg0)).into(),
                ))
            }
            470usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_total_climb")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_total_climb(&arg0),
                ))
            }
            471usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_total_descent")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint3d_ops::tgeompoint3d_total_descent(&arg0),
                ))
            }
            472usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_value_at_x")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint3d_value_at_x")?;
                Ok(
                    match tgeompoint3d_ops::tgeompoint3d_value_at_x(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            473usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_value_at_y")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint3d_value_at_y")?;
                Ok(
                    match tgeompoint3d_ops::tgeompoint3d_value_at_y(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            474usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_value_at_z")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint3d_value_at_z")?;
                Ok(
                    match tgeompoint3d_ops::tgeompoint3d_value_at_z(&arg0, arg1) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            475usize => {
                let arg0 =
                    arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_vertical_speed")?;
                match tgeompoint3d_ops::tgeompoint3d_vertical_speed(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            476usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_acceleration_count")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_acceleration_count")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_acceleration_count(&arg0, arg1) as i64,
                ))
            }
            477usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(
                    &args,
                    0,
                    "tgeompoint_acceleration_event_count",
                )?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_acceleration_event_count")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_acceleration_event_count(&arg0, arg1)
                        as i64,
                ))
            }
            478usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_acceleration_events")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_acceleration_events")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_acceleration_events(&arg0, arg1) as i64,
                ))
            }
            479usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_angular_difference")?;
                match tgeompoint_ops::tgeompoint_angular_difference(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            480usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_at_period")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint_at_period")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_at_period")?;
                match tgeompoint_extended_ops::tgeompoint_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            481usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_at_point")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_at_point")?;
                let arg2 = dv_f64(&args, 2, "tgeompoint_at_point")?;
                let arg3 = dv_f64(&args, 3, "tgeompoint_at_point")?;
                match trajectory_restrictions_ops::tgeompoint_at_point(&arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            482usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_at_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "tgeompoint_at_stbox")?;
                match trajectory_restrictions_ops::tgeompoint_at_stbox(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            483usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_avg_speed")?;
                Ok(match tgeompoint_ops::tgeompoint_avg_speed(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            484usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_azimuth")?;
                match tgeompoint_ops::tgeompoint_azimuth(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            485usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_bearing")?;
                {
                    let __r = tgeompoint_extended_ops::tgeompoint_bearing(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            486usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_bounding_box")?;
                match tgeompoint_ops::tgeompoint_bounding_box(&arg0) {
                    Some(__rec) => ret_to_witvalue_bounding_box(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            487usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_cluster_count_dbscan")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_cluster_count_dbscan")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_cluster_count_dbscan")? as u32;
                Ok(types::Duckvalue::Int64(
                    clustering_ops::tgeompoint_cluster_count_dbscan(&arg0, arg1, arg2) as i64,
                ))
            }
            488usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_cumulative_length")?;
                match tgeompoint_ops::tgeompoint_cumulative_length(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            489usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_detect_loops")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_detect_loops")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_detect_loops(&arg0, arg1) as i64,
                ))
            }
            490usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_detect_stops")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_detect_stops")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_detect_stops")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_detect_stops(&arg0, arg1, arg2) as i64,
                ))
            }
            491usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_detect_turns")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_detect_turns")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_detect_turns(&arg0, arg1) as i64,
                ))
            }
            492usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_detect_u_turns")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_detection_ops::tgeompoint_detect_u_turns(&arg0) as i64,
                ))
            }
            493usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_direction_stats")?;
                match tgeompoint_ops::tgeompoint_direction_stats(&arg0) {
                    Some(__rec) => ret_to_witvalue_direction_stats(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            494usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_distance_at")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "tgeompoint_distance_at")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_distance_at")?;
                Ok(
                    match tgeompoint_ops::tgeompoint_distance_at(&arg0, &arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            495usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_distance_to_point")?;
                let arg1 = arg_witvalue_geom_point(&args, 1, "tgeompoint_distance_to_point")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_distance_to_point")?;
                Ok(
                    match tgeompoint_ops::tgeompoint_distance_to_point(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            496usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_from_web_mercator")?;
                match tgeompoint_extended_ops::tgeompoint_from_web_mercator(&arg0) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            497usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_full_analysis")?;
                ret_to_witvalue_trajectory_analysis(tgeompoint_ops::tgeompoint_full_analysis(&arg0))
            }
            498usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_geodetic_length")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint_extended_ops::tgeompoint_geodetic_length(&arg0),
                ))
            }
            499usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_geodetic_speed")?;
                {
                    let __r = tgeompoint_extended_ops::tgeompoint_geodetic_speed(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            500usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_length")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint_ops::tgeompoint_length(&arg0),
                ))
            }
            501usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_max_speed")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint_ops::tgeompoint_max_speed(&arg0),
                ))
            }
            502usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_min_speed")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint_ops::tgeompoint_min_speed(&arg0),
                ))
            }
            503usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_minus_period")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint_minus_period")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_minus_period")?;
                match tgeompoint_extended_ops::tgeompoint_minus_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            504usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_minus_point")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_minus_point")?;
                let arg2 = dv_f64(&args, 2, "tgeompoint_minus_point")?;
                let arg3 = dv_f64(&args, 3, "tgeompoint_minus_point")?;
                match trajectory_restrictions_ops::tgeompoint_minus_point(&arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            505usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_minus_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "tgeompoint_minus_stbox")?;
                match trajectory_restrictions_ops::tgeompoint_minus_stbox(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            506usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_movement_summary")?;
                ret_to_witvalue_movement_summary(tgeompoint_ops::tgeompoint_movement_summary(&arg0))
            }
            507usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(
                    &args,
                    0,
                    "tgeompoint_nearest_approach_distance",
                )?;
                let arg1 = arg_witvalue_tgeompoint_sequence(
                    &args,
                    1,
                    "tgeompoint_nearest_approach_distance",
                )?;
                Ok(
                    match trajectory_restrictions_ops::tgeompoint_nearest_approach_distance(
                        &arg0, &arg1,
                    ) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            508usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_ops::tgeompoint_num_instants(&arg0) as i64,
                ))
            }
            509usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_num_loops")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_num_loops")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeompoint_num_loops(&arg0, arg1) as i64,
                ))
            }
            510usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_num_stops")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_num_stops")?;
                let arg2 = dv_i64(&args, 2, "tgeompoint_num_stops")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeompoint_num_stops(&arg0, arg1, arg2) as i64,
                ))
            }
            511usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_num_turns")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_num_turns")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeompoint_num_turns(&arg0, arg1) as i64,
                ))
            }
            512usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_num_u_turns")?;
                Ok(types::Duckvalue::Int64(
                    analytics_ops::tgeompoint_num_u_turns(&arg0) as i64,
                ))
            }
            513usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_overlaps_stbox")?;
                let arg1 = arg_witvalue_stbox(&args, 1, "tgeompoint_overlaps_stbox")?;
                Ok(types::Duckvalue::Boolean(
                    tgeompoint_extended_ops::tgeompoint_overlaps_stbox(&arg0, arg1),
                ))
            }
            514usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_quality_score")?;
                Ok(types::Duckvalue::Float64(
                    analytics_ops::tgeompoint_quality_score(&arg0),
                ))
            }
            515usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_simplify")?;
                let arg1 = dv_f64(&args, 1, "tgeompoint_simplify")?;
                ret_to_witvalue_tgeompoint_sequence(
                    tgeompoint_ops::tgeompoint_simplify(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "tgeompoint_simplify: {}",
                            shim_err_string(e)
                        ))
                    })?,
                )
            }
            516usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_speed_stats")?;
                match tgeompoint_ops::tgeompoint_speed_stats(&arg0) {
                    Some(__rec) => ret_to_witvalue_speed_stats(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            517usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_speeds")?;
                match tgeompoint_ops::tgeompoint_speeds(&arg0) {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            518usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_srid")?;
                Ok(types::Duckvalue::Int64(
                    tgeompoint_extended_ops::tgeompoint_srid(&arg0) as i64,
                ))
            }
            519usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_to_geojson")?;
                Ok(types::Duckvalue::Text(
                    (serde_ops::tgeompoint_to_geojson(&arg0)).into(),
                ))
            }
            520usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_to_geojson_feature")?;
                Ok(types::Duckvalue::Text(
                    (serde_ops::tgeompoint_to_geojson_feature(&arg0)).into(),
                ))
            }
            521usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_to_web_mercator")?;
                match tgeompoint_extended_ops::tgeompoint_to_web_mercator(&arg0) {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            522usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_tortuosity")?;
                Ok(types::Duckvalue::Float64(
                    tgeompoint_ops::tgeompoint_tortuosity(&arg0),
                ))
            }
            523usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_value_at")?;
                let arg1 = dv_i64(&args, 1, "tgeompoint_value_at")?;
                match tgeompoint_ops::tgeompoint_value_at(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_geom_point(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            524usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_abs")?;
                ret_to_witvalue_tint_sequence(tint_math_ops::tint_abs(&arg0))
            }
            525usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_add")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_add")?;
                match arithmetic_ops::tint_add(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            526usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_add_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_add_scalar")?;
                ret_to_witvalue_tint_sequence(arithmetic_ops::tint_add_scalar(&arg0, arg1))
            }
            527usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_eq")?;
                let arg1 = dv_i64(&args, 1, "tint_always_eq")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_eq(
                    &arg0, arg1,
                )))
            }
            528usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_eq_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_always_eq_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_eq(
                    &arg0, arg1,
                )))
            }
            529usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_gt")?;
                let arg1 = dv_i64(&args, 1, "tint_always_gt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_gt(
                    &arg0, arg1,
                )))
            }
            530usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_gt_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_always_gt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_gt(
                    &arg0, arg1,
                )))
            }
            531usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_lt")?;
                let arg1 = dv_i64(&args, 1, "tint_always_lt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_lt(
                    &arg0, arg1,
                )))
            }
            532usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_always_lt_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_always_lt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_always_lt(
                    &arg0, arg1,
                )))
            }
            533usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_at_max")?;
                match restrictions_ops::tint_at_max(&arg0) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            534usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_at_min")?;
                match restrictions_ops::tint_at_min(&arg0) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            535usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_at_period")?;
                let arg1 = dv_i64(&args, 1, "tint_at_period")?;
                let arg2 = dv_i64(&args, 2, "tint_at_period")?;
                match restrictions_ops::tint_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            536usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_at_value")?;
                let arg1 = dv_i64(&args, 1, "tint_at_value")?;
                match restrictions_ops::tint_at_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            537usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_diff")?;
                ret_to_witvalue_tint_sequence(tint_ops::tint_diff(&arg0))
            }
            538usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_div")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_div")?;
                match arithmetic_ops::tint_div(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            539usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_div_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_div_scalar")?;
                match arithmetic_ops::tint_div_scalar(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            540usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_eq")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_eq")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_eq(
                    &arg0, arg1,
                )))
            }
            541usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_eq_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_eq_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_eq(
                    &arg0, arg1,
                )))
            }
            542usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_gt")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_gt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_gt(
                    &arg0, arg1,
                )))
            }
            543usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_gt_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_gt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_gt(
                    &arg0, arg1,
                )))
            }
            544usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_lt")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_lt")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_lt(
                    &arg0, arg1,
                )))
            }
            545usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_ever_lt_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_ever_lt_scalar")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::tint_ever_lt(
                    &arg0, arg1,
                )))
            }
            546usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_exponential_smooth")?;
                let arg1 = dv_f64(&args, 1, "tint_exponential_smooth")?;
                ret_to_witvalue_tfloat_sequence(tint_math_ops::tint_exponential_smooth(&arg0, arg1))
            }
            547usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_integral")?;
                Ok(types::Duckvalue::Int64(
                    tint_math_ops::tint_integral(&arg0) as i64
                ))
            }
            548usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_max_value_scalar")?;
                Ok(match tint_ops::tint_max_value(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            549usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_max_value_wit")?;
                Ok(match tint_ops::tint_max_value(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            550usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_min_value_scalar")?;
                Ok(match tint_ops::tint_min_value(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            551usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_min_value_wit")?;
                Ok(match tint_ops::tint_min_value(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            552usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_minus_period")?;
                let arg1 = dv_i64(&args, 1, "tint_minus_period")?;
                let arg2 = dv_i64(&args, 2, "tint_minus_period")?;
                match restrictions_ops::tint_minus_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            553usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_minus_value")?;
                let arg1 = dv_i64(&args, 1, "tint_minus_value")?;
                match restrictions_ops::tint_minus_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            554usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_mod_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_mod_scalar")?;
                match tint_math_ops::tint_mod_scalar(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            555usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_moving_average")?;
                let arg1 = dv_i64(&args, 1, "tint_moving_average")?;
                ret_to_witvalue_tfloat_sequence(tint_math_ops::tint_moving_average(&arg0, arg1))
            }
            556usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_mul")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_mul")?;
                match arithmetic_ops::tint_mul(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            557usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_mul_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_mul_scalar")?;
                ret_to_witvalue_tint_sequence(arithmetic_ops::tint_mul_scalar(&arg0, arg1))
            }
            558usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_negate")?;
                ret_to_witvalue_tint_sequence(tint_math_ops::tint_negate(&arg0))
            }
            559usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tint_ops::tint_num_instants(&arg0) as i64
                ))
            }
            560usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_num_instants_wit")?;
                Ok(types::Duckvalue::Int64(
                    tint_ops::tint_num_instants(&arg0) as i64
                ))
            }
            561usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_sequence_to_tfloat")?;
                ret_to_witvalue_tfloat_sequence(tint_math_ops::tint_sequence_to_tfloat(&arg0))
            }
            562usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_sub")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_sub")?;
                match arithmetic_ops::tint_sub(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            563usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_sub_scalar")?;
                let arg1 = dv_i64(&args, 1, "tint_sub_scalar")?;
                ret_to_witvalue_tint_sequence(arithmetic_ops::tint_sub_scalar(&arg0, arg1))
            }
            564usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_eq")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_eq")?;
                match comparisons_ops::tint_temporal_eq(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            565usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_ge")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_ge")?;
                match comparisons_ops::tint_temporal_ge(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            566usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_gt")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_gt")?;
                match comparisons_ops::tint_temporal_gt(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            567usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_le")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_le")?;
                match comparisons_ops::tint_temporal_le(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            568usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_lt")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_lt")?;
                match comparisons_ops::tint_temporal_lt(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            569usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_temporal_ne")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "tint_temporal_ne")?;
                match comparisons_ops::tint_temporal_ne(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            570usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_time_span")?;
                match tint_ops::tint_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            571usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_to_json")?;
                Ok(types::Duckvalue::Text(
                    (serde_ops::tint_to_json(&arg0)).into(),
                ))
            }
            572usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twavg")?;
                Ok(match tint_math_ops::tint_twavg(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            573usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twcount")?;
                Ok(types::Duckvalue::Float64(tint_ops::tint_twcount(&arg0)))
            }
            574usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twmax")?;
                Ok(match tint_math_ops::tint_twmax(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            575usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twmin")?;
                Ok(match tint_math_ops::tint_twmin(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            576usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twstddev")?;
                Ok(match tint_ops::tint_twstddev(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            577usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twsum")?;
                Ok(types::Duckvalue::Int64(
                    tint_math_ops::tint_twsum(&arg0) as i64
                ))
            }
            578usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_twvariance")?;
                Ok(match tint_ops::tint_twvariance(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            579usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_value_at")?;
                let arg1 = dv_i64(&args, 1, "tint_value_at")?;
                Ok(match tint_ops::tint_value_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            580usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_eq")?;
                let arg1 = dv_i64(&args, 1, "tint_when_eq")?;
                match restrictions_ops::tint_when_eq(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            581usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_ge")?;
                let arg1 = dv_i64(&args, 1, "tint_when_ge")?;
                match restrictions_ops::tint_when_ge(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            582usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_gt")?;
                let arg1 = dv_i64(&args, 1, "tint_when_gt")?;
                match restrictions_ops::tint_when_gt(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            583usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_le")?;
                let arg1 = dv_i64(&args, 1, "tint_when_le")?;
                match restrictions_ops::tint_when_le(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            584usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_lt")?;
                let arg1 = dv_i64(&args, 1, "tint_when_lt")?;
                match restrictions_ops::tint_when_lt(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            585usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_when_ne")?;
                let arg1 = dv_i64(&args, 1, "tint_when_ne")?;
                match restrictions_ops::tint_when_ne(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            586usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_at_period")?;
                let arg1 = dv_i64(&args, 1, "tnpoint_at_period")?;
                let arg2 = dv_i64(&args, 2, "tnpoint_at_period")?;
                match tnpoint_extended_ops::tnpoint_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            587usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_at_position_range")?;
                let arg1 = dv_f64(&args, 1, "tnpoint_at_position_range")?;
                let arg2 = dv_f64(&args, 2, "tnpoint_at_position_range")?;
                match tnpoint_extended_ops::tnpoint_at_position_range(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            588usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_avg_network_speed")?;
                Ok(match tnpoint_ops::tnpoint_avg_network_speed(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            589usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_cumulative_length")?;
                ret_to_witvalue_tfloat_sequence(tnpoint_extended_ops::tnpoint_cumulative_length(
                    &arg0,
                ))
            }
            590usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_end_position")?;
                Ok(match tnpoint_ops::tnpoint_end_position(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            591usize => {
                let arg0 = dv_text(&args, 0, "tnpoint_from_ewkt")?;
                match tnpoint_ops::tnpoint_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            592usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_is_backward")?;
                Ok(types::Duckvalue::Boolean(tnpoint_ops::tnpoint_is_backward(
                    &arg0,
                )))
            }
            593usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_is_forward")?;
                Ok(types::Duckvalue::Boolean(tnpoint_ops::tnpoint_is_forward(
                    &arg0,
                )))
            }
            594usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_is_single_route")?;
                Ok(types::Duckvalue::Boolean(
                    tnpoint_ops::tnpoint_is_single_route(&arg0),
                ))
            }
            595usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_max_position")?;
                Ok(match tnpoint_ops::tnpoint_max_position(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            596usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_min_position")?;
                Ok(match tnpoint_ops::tnpoint_min_position(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            597usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_network_length")?;
                Ok(types::Duckvalue::Float64(
                    tnpoint_ops::tnpoint_network_length(&arg0),
                ))
            }
            598usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_network_speed")?;
                ret_to_witvalue_tfloat_sequence(tnpoint_extended_ops::tnpoint_network_speed(&arg0))
            }
            599usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tnpoint_ops::tnpoint_num_instants(&arg0) as i64,
                ))
            }
            600usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_num_route_changes")?;
                Ok(types::Duckvalue::Int64(
                    tnpoint_extended_ops::tnpoint_num_route_changes(&arg0) as i64,
                ))
            }
            601usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_num_stops")?;
                let arg1 = dv_f64(&args, 1, "tnpoint_num_stops")?;
                let arg2 = dv_i64(&args, 2, "tnpoint_num_stops")?;
                Ok(types::Duckvalue::Int64(
                    tnpoint_extended_ops::tnpoint_num_stops(&arg0, arg1, arg2) as i64,
                ))
            }
            602usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_position_at")?;
                let arg1 = dv_i64(&args, 1, "tnpoint_position_at")?;
                Ok(match tnpoint_ops::tnpoint_position_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            603usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_positions")?;
                ret_to_witvalue_tfloat_sequence(tnpoint_extended_ops::tnpoint_positions(&arg0))
            }
            604usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_route_id")?;
                Ok(match tnpoint_ops::tnpoint_route_id(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            605usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_scale_time")?;
                let arg1 = dv_f64(&args, 1, "tnpoint_scale_time")?;
                match tnpoint_extended_ops::tnpoint_scale_time(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            606usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_shift_time")?;
                let arg1 = dv_i64(&args, 1, "tnpoint_shift_time")?;
                match tnpoint_extended_ops::tnpoint_shift_time(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            607usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_simplify")?;
                let arg1 = dv_f64(&args, 1, "tnpoint_simplify")?;
                match tnpoint_extended_ops::tnpoint_simplify(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tnpoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            608usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_start_position")?;
                Ok(match tnpoint_ops::tnpoint_start_position(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            609usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_stop_duration")?;
                let arg1 = dv_f64(&args, 1, "tnpoint_stop_duration")?;
                let arg2 = dv_i64(&args, 2, "tnpoint_stop_duration")?;
                Ok(types::Duckvalue::Int64(
                    tnpoint_extended_ops::tnpoint_stop_duration(&arg0, arg1, arg2) as i64,
                ))
            }
            610usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_time_span")?;
                match tnpoint_ops::tnpoint_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            611usize => {
                let arg0 = arg_witvalue_tnpoint_sequence(&args, 0, "tnpoint_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (tnpoint_ops::tnpoint_to_ewkt(&arg0)).into(),
                ))
            }
            612usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_angular_acceleration")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_angular_acceleration(
                    &arg0,
                ))
            }
            613usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_angular_distance_to_pose")?;
                let arg1 = arg_witvalue_tpose_sequence(&args, 1, "tpose_angular_distance_to_pose")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_angular_distance_to_pose(
                    &arg0, &arg1,
                ))
            }
            614usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_angular_velocity")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_angular_velocity(&arg0))
            }
            615usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_avg_orientation")?;
                Ok(match tpose_ops::tpose_avg_orientation(&arg0) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            616usize => {
                let arg0 =
                    arg_witvalue_tpose_sequence(&args, 0, "tpose_compute_heading_from_movement")?;
                ret_to_witvalue_tfloat_sequence(
                    tpose_extended_ops::tpose_compute_heading_from_movement(&arg0),
                )
            }
            617usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_curvature")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_curvature(&arg0))
            }
            618usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_distance_to_point")?;
                let arg1 = dv_f64(&args, 1, "tpose_distance_to_point")?;
                let arg2 = dv_f64(&args, 2, "tpose_distance_to_point")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_distance_to_point(
                    &arg0, arg1, arg2,
                ))
            }
            619usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_distance_to_pose")?;
                let arg1 = arg_witvalue_tpose_sequence(&args, 1, "tpose_distance_to_pose")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_distance_to_pose(
                    &arg0, &arg1,
                ))
            }
            620usize => {
                let arg0 = dv_text(&args, 0, "tpose_from_ewkt")?;
                match tpose_ops::tpose_from_ewkt(arg0) {
                    Some(__rec) => ret_to_witvalue_tpose_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            621usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_get_orientation")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_get_orientation(&arg0))
            }
            622usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_get_x")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_get_x(&arg0))
            }
            623usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_get_y")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_get_y(&arg0))
            }
            624usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_linear_acceleration")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_linear_acceleration(
                    &arg0,
                ))
            }
            625usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_linear_speed")?;
                ret_to_witvalue_tfloat_sequence(tpose_extended_ops::tpose_linear_speed(&arg0))
            }
            626usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_nearest_approach")?;
                let arg1 = arg_witvalue_tpose_sequence(&args, 1, "tpose_nearest_approach")?;
                Ok(match tpose_ops::tpose_nearest_approach(&arg0, &arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            627usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_nearest_approach_point")?;
                let arg1 = dv_f64(&args, 1, "tpose_nearest_approach_point")?;
                let arg2 = dv_f64(&args, 2, "tpose_nearest_approach_point")?;
                Ok(
                    match tpose_ops::tpose_nearest_approach_point(&arg0, arg1, arg2) {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            628usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    tpose_ops::tpose_num_instants(&arg0) as i64,
                ))
            }
            629usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_path_length")?;
                Ok(types::Duckvalue::Float64(tpose_ops::tpose_path_length(
                    &arg0,
                )))
            }
            630usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_pose_at_orientation")?;
                let arg1 = dv_i64(&args, 1, "tpose_pose_at_orientation")?;
                Ok(match tpose_ops::tpose_pose_at_orientation(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            631usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_pose_at_x")?;
                let arg1 = dv_i64(&args, 1, "tpose_pose_at_x")?;
                Ok(match tpose_ops::tpose_pose_at_x(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            632usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_pose_at_y")?;
                let arg1 = dv_i64(&args, 1, "tpose_pose_at_y")?;
                Ok(match tpose_ops::tpose_pose_at_y(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            633usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_time_span")?;
                match tpose_ops::tpose_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            634usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_to_ewkt")?;
                Ok(types::Duckvalue::Text(
                    (tpose_ops::tpose_to_ewkt(&arg0)).into(),
                ))
            }
            635usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_total_rotation")?;
                Ok(types::Duckvalue::Float64(tpose_ops::tpose_total_rotation(
                    &arg0,
                )))
            }
            636usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_contains")?;
                let arg1 = dv_i64(&args, 1, "tstzset_contains")?;
                Ok(types::Duckvalue::Boolean(tstzset_ops::tstzset_contains(
                    &arg0, arg1,
                )))
            }
            637usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_intersection")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "tstzset_intersection")?;
                {
                    let __r = tstzset_ops::tstzset_intersection(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            638usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_len")?;
                Ok(types::Duckvalue::Int64(
                    tstzset_ops::tstzset_len(&arg0) as i64
                ))
            }
            639usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_make")?;
                {
                    let __r = tstzset_ops::tstzset_make(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            640usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_minus")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "tstzset_minus")?;
                {
                    let __r = tstzset_ops::tstzset_minus(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            641usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_nth")?;
                let arg1 = dv_i64(&args, 1, "tstzset_nth")? as u32;
                Ok(match tstzset_ops::tstzset_nth(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            642usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_overlaps")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "tstzset_overlaps")?;
                Ok(types::Duckvalue::Boolean(tstzset_ops::tstzset_overlaps(
                    &arg0, &arg1,
                )))
            }
            643usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_shift")?;
                let arg1 = dv_i64(&args, 1, "tstzset_shift")?;
                {
                    let __r = tstzset_ops::tstzset_shift(&arg0, arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            644usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_successor")?;
                {
                    let __r = tstzset_ops::tstzset_successor(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            645usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (sets_ops::tstz_set_to_text(&arg0)).into(),
                ))
            }
            646usize => {
                let arg0: Vec<i64> = parse_json_list_i64(&args, 0, "tstzset_union")?;
                let arg1: Vec<i64> = parse_json_list_i64(&args, 1, "tstzset_union")?;
                {
                    let __r = tstzset_ops::tstzset_union(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            647usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_adjacent")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_adjacent")?;
                Ok(types::Duckvalue::Boolean(tstzspan_ops::tstzspan_adjacent(
                    arg0, arg1,
                )))
            }
            648usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_contains")?;
                let arg1 = dv_i64(&args, 1, "tstzspan_contains")?;
                Ok(types::Duckvalue::Boolean(tstzspan_ops::tstzspan_contains(
                    arg0, arg1,
                )))
            }
            649usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_contains_span")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_contains_span")?;
                Ok(types::Duckvalue::Boolean(
                    tstzspan_ops::tstzspan_contains_span(arg0, arg1),
                ))
            }
            650usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_duration")?;
                Ok(types::Duckvalue::Int64(
                    tstzspan_ops::tstzspan_duration(arg0) as i64,
                ))
            }
            651usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_end")?;
                Ok(types::Duckvalue::Int64(
                    tstzspan_ops::tstzspan_end(arg0) as i64
                ))
            }
            652usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_expand")?;
                let arg1 = dv_i64(&args, 1, "tstzspan_expand")?;
                match tstzspan_ops::tstzspan_expand(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            653usize => {
                let arg0 = dv_text(&args, 0, "tstzspan_from_text")?;
                match spans_ops::tstz_span_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_int_span(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            654usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_intersection")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_intersection")?;
                match tstzspan_ops::tstzspan_intersection(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            655usize => {
                let arg0 = dv_i64(&args, 0, "tstzspan_make")?;
                let arg1 = dv_i64(&args, 1, "tstzspan_make")?;
                let arg2 = dv_bool(&args, 2, "tstzspan_make")?;
                let arg3 = dv_bool(&args, 3, "tstzspan_make")?;
                match tstzspan_ops::tstzspan_make(arg0, arg1, arg2, arg3) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            656usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_midpoint")?;
                Ok(types::Duckvalue::Int64(
                    tstzspan_ops::tstzspan_midpoint(arg0) as i64,
                ))
            }
            657usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_minus")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_minus")?;
                match tstzspan_ops::tstzspan_minus(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            658usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_overlaps")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_overlaps")?;
                Ok(types::Duckvalue::Boolean(tstzspan_ops::tstzspan_overlaps(
                    arg0, arg1,
                )))
            }
            659usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_scale")?;
                let arg1 = dv_f64(&args, 1, "tstzspan_scale")?;
                let arg2 = dv_i64(&args, 2, "tstzspan_scale")?;
                match tstzspan_ops::tstzspan_scale(arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            660usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_shift")?;
                let arg1 = dv_i64(&args, 1, "tstzspan_shift")?;
                ret_to_witvalue_tstzspan(tstzspan_ops::tstzspan_shift(arg0, arg1))
            }
            661usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_start")?;
                Ok(types::Duckvalue::Int64(
                    tstzspan_ops::tstzspan_start(arg0) as i64
                ))
            }
            662usize => {
                let arg0 = arg_witvalue_int_span(&args, 0, "tstzspan_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::tstz_span_to_text(arg0)).into(),
                ))
            }
            663usize => {
                let arg0 = arg_witvalue_tstzspan(&args, 0, "tstzspan_union")?;
                let arg1 = arg_witvalue_tstzspan(&args, 1, "tstzspan_union")?;
                match tstzspan_ops::tstzspan_union(arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tstzspan(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            664usize => {
                let arg0 = dv_text(&args, 0, "tstzspanset_from_text")?;
                {
                    match spans_ops::tstz_spanset_from_text(arg0) {
                        Some(__v) => {
                            let __json = serde_json::to_string(&__v).map_err(|e| {
                                types::Duckerror::Internal(format!(
                                    "tstzspanset_from_text: encode JSON: {}",
                                    e
                                ))
                            })?;
                            Ok(types::Duckvalue::Text(__json))
                        }
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            665usize => {
                let arg0 = parse_json_list_record_int_span(&args, 0, "tstzspanset_to_text")?;
                Ok(types::Duckvalue::Text(
                    (spans_ops::tstz_spanset_to_text(&arg0)).into(),
                ))
            }
            666usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_always_eq")?;
                let arg1 = dv_text(&args, 1, "ttext_always_eq")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::ttext_always_eq(
                    &arg0, arg1,
                )))
            }
            667usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_at_period")?;
                let arg1 = dv_i64(&args, 1, "ttext_at_period")?;
                let arg2 = dv_i64(&args, 2, "ttext_at_period")?;
                match restrictions_ops::ttext_at_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            668usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_at_value")?;
                let arg1 = dv_text(&args, 1, "ttext_at_value")?;
                match restrictions_ops::ttext_at_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            669usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_btrim")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_btrim(&arg0))
            }
            670usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_concat")?;
                let arg1 = arg_witvalue_ttext_sequence(&args, 1, "ttext_concat")?;
                match ttext_strings_ops::ttext_concat(&arg0, &arg1) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            671usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_concat_str")?;
                let arg1 = dv_text(&args, 1, "ttext_concat_str")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_concat_str(&arg0, arg1))
            }
            672usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_duration")?;
                Ok(types::Duckvalue::Int64(
                    ttext_ops::ttext_duration(&arg0) as i64
                ))
            }
            673usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_end_timestamp")?;
                Ok(match ttext_ops::ttext_end_timestamp(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            674usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_end_value")?;
                Ok(match ttext_ops::ttext_end_value(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            675usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_ever_eq")?;
                let arg1 = dv_text(&args, 1, "ttext_ever_eq")?;
                Ok(types::Duckvalue::Boolean(comparisons_ops::ttext_ever_eq(
                    &arg0, arg1,
                )))
            }
            676usize => {
                let arg0 = dv_text(&args, 0, "ttext_from_csv")?;
                match ttext_ops::ttext_from_csv(arg0) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            677usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_ilike")?;
                let arg1 = dv_text(&args, 1, "ttext_ilike")?;
                ret_to_witvalue_tbool_sequence(ttext_strings_ops::ttext_ilike(&arg0, arg1))
            }
            678usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_left")?;
                let arg1 = dv_i64(&args, 1, "ttext_left")? as u32;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_left(&arg0, arg1))
            }
            679usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_length")?;
                ret_to_witvalue_tint_sequence(ttext_strings_ops::ttext_length(&arg0))
            }
            680usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_like")?;
                let arg1 = dv_text(&args, 1, "ttext_like")?;
                ret_to_witvalue_tbool_sequence(ttext_strings_ops::ttext_like(&arg0, arg1))
            }
            681usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_lower")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_lower(&arg0))
            }
            682usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_ltrim")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_ltrim(&arg0))
            }
            683usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_minus_period")?;
                let arg1 = dv_i64(&args, 1, "ttext_minus_period")?;
                let arg2 = dv_i64(&args, 2, "ttext_minus_period")?;
                match restrictions_ops::ttext_minus_period(&arg0, arg1, arg2) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            684usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_minus_value")?;
                let arg1 = dv_text(&args, 1, "ttext_minus_value")?;
                match restrictions_ops::ttext_minus_value(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            685usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_num_instants")?;
                Ok(types::Duckvalue::Int64(
                    ttext_ops::ttext_num_instants(&arg0) as i64,
                ))
            }
            686usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_num_instants_scalar")?;
                Ok(types::Duckvalue::Int64(
                    ttext_ops::ttext_num_instants(&arg0) as i64,
                ))
            }
            687usize => {
                let arg0 = dv_text(&args, 0, "ttext_prepend_str")?;
                let arg1 = arg_witvalue_ttext_sequence(&args, 1, "ttext_prepend_str")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_prepend_str(arg0, &arg1))
            }
            688usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_regex_match")?;
                let arg1 = dv_text(&args, 1, "ttext_regex_match")?;
                ret_to_witvalue_tbool_sequence(ttext_strings_ops::ttext_regex_match(&arg0, arg1))
            }
            689usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_regex_replace")?;
                let arg1 = dv_text(&args, 1, "ttext_regex_replace")?;
                let arg2 = dv_text(&args, 2, "ttext_regex_replace")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_regex_replace(
                    &arg0, arg1, arg2,
                ))
            }
            690usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_right")?;
                let arg1 = dv_i64(&args, 1, "ttext_right")? as u32;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_right(&arg0, arg1))
            }
            691usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_rtrim")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_rtrim(&arg0))
            }
            692usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_start_timestamp")?;
                Ok(match ttext_ops::ttext_start_timestamp(&arg0) {
                    Some(v) => types::Duckvalue::Int64(v as i64),
                    None => types::Duckvalue::Null,
                })
            }
            693usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_start_value")?;
                Ok(match ttext_ops::ttext_start_value(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            694usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_time_span")?;
                match ttext_ops::ttext_time_span(&arg0) {
                    Some(__rec) => ret_to_witvalue_time_period(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            695usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_to_csv")?;
                Ok(types::Duckvalue::Text(
                    (ttext_ops::ttext_to_csv(&arg0)).into(),
                ))
            }
            696usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_to_json")?;
                Ok(types::Duckvalue::Text(
                    (ttext_ops::ttext_to_json(&arg0)).into(),
                ))
            }
            697usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_upper")?;
                ret_to_witvalue_ttext_sequence(ttext_strings_ops::ttext_upper(&arg0))
            }
            698usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_value_at")?;
                let arg1 = dv_i64(&args, 1, "ttext_value_at")?;
                Ok(match ttext_ops::ttext_value_at(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            699usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_when_eq")?;
                let arg1 = dv_text(&args, 1, "ttext_when_eq")?;
                match restrictions_ops::ttext_when_eq(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            700usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "ttext_when_ne")?;
                let arg1 = dv_text(&args, 1, "ttext_when_ne")?;
                match restrictions_ops::ttext_when_ne(&arg0, arg1) {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            701usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "twed_distance")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "twed_distance")?;
                let arg2 = dv_f64(&args, 2, "twed_distance")?;
                let arg3 = dv_f64(&args, 3, "twed_distance")?;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::twed_distance(&arg0, &arg1, arg2, arg3),
                ))
            }
            702usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "twed_distance_normalized")?;
                let arg1 = arg_witvalue_tgeompoint_sequence(&args, 1, "twed_distance_normalized")?;
                let arg2 = dv_f64(&args, 2, "twed_distance_normalized")?;
                let arg3 = dv_f64(&args, 3, "twed_distance_normalized")?;
                Ok(types::Duckvalue::Float64(
                    similarity_extended_ops::twed_distance_normalized(&arg0, &arg1, arg2, arg3),
                ))
            }
            703usize => {
                let arg0 = dv_text(&args, 0, "stbox3d_from_text")?;
                match stbox_ops::stbox3d_from_text(arg0) {
                    Some(__rec) => ret_to_witvalue_stbox3d(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown scalar arm index {}",
                arm_idx
            ))),
        }
    }

    fn call_table(
        handle: u32,
        args: Vec<types::Duckvalue>,
    ) -> Result<types::Resultset, types::Duckerror> {
        let arm_idx = table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown table function handle".into()))?;
        match arm_idx {
            0usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "as_of_join_bool")?;
                let arg1 = arg_witvalue_tbool_sequence(&args, 1, "as_of_join_bool")?;
                let __upstream = typed_join_ops::as_of_join_bool(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            1usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "as_of_join_float")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "as_of_join_float")?;
                let __upstream = join_ops::as_of_join_float(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.left as f64),
                        types::Duckvalue::Float64(__row.right as f64)
                    ]);
                }
                Ok(rows)
            }
            2usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "as_of_join_int")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "as_of_join_int")?;
                let __upstream = typed_join_ops::as_of_join_int(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            3usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "as_of_join_text")?;
                let arg1 = arg_witvalue_ttext_sequence(&args, 1, "as_of_join_text")?;
                let __upstream = typed_join_ops::as_of_join_text(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Text(__row.left.clone()),
                        types::Duckvalue::Text(__row.right.clone())
                    ]);
                }
                Ok(rows)
            }
            4usize => Err(types::Duckerror::Unsupported(format!(
                "interval_tree_query_overlapping: UDTF param #0 (list<record>) not wired"
            ))),
            5usize => Err(types::Duckerror::Unsupported(format!(
                "interval_tree_query_point: UDTF param #0 (list<record>) not wired"
            ))),
            6usize => Err(types::Duckerror::Unsupported(format!(
                "kdtree_xy_nearest_k: UDTF param #0 (list<record>) not wired"
            ))),
            7usize => Err(types::Duckerror::Unsupported(format!(
                "kdtree_xy_within: UDTF param #0 (list<record>) not wired"
            ))),
            8usize => Err(types::Duckerror::Unsupported(format!(
                "kdtree_xyz_nearest_k: UDTF param #0 (list<record>) not wired"
            ))),
            9usize => Err(types::Duckerror::Unsupported(format!(
                "kdtree_xyz_within: UDTF param #0 (list<record>) not wired"
            ))),
            10usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "nearest_join_bool")?;
                let arg1 = arg_witvalue_tbool_sequence(&args, 1, "nearest_join_bool")?;
                let __upstream = nearest_join_ops::nearest_join_bool(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.distance as f64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            11usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "nearest_join_float")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "nearest_join_float")?;
                let __upstream = nearest_join_ops::nearest_join_float(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.distance as f64),
                        types::Duckvalue::Float64(__row.left as f64),
                        types::Duckvalue::Float64(__row.right as f64)
                    ]);
                }
                Ok(rows)
            }
            12usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "nearest_join_int")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "nearest_join_int")?;
                let __upstream = nearest_join_ops::nearest_join_int(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.distance as f64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            13usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "nearest_join_text")?;
                let arg1 = arg_witvalue_ttext_sequence(&args, 1, "nearest_join_text")?;
                let __upstream = nearest_join_ops::nearest_join_text(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.distance as f64),
                        types::Duckvalue::Text(__row.left.clone()),
                        types::Duckvalue::Text(__row.right.clone())
                    ]);
                }
                Ok(rows)
            }
            14usize => Err(types::Duckerror::Unsupported(format!(
                "octree_query_box: UDTF param #0 (list<record>) not wired"
            ))),
            15usize => Err(types::Duckerror::Unsupported(format!(
                "octree_query_sphere: UDTF param #0 (list<record>) not wired"
            ))),
            16usize => Err(types::Duckerror::Unsupported(format!(
                "quadtree_query_box: UDTF param #0 (list<record>) not wired"
            ))),
            17usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "sequence_json")?;
                let __upstream = sequence_json_ops::sequence_json(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.seq_id as i64),
                        types::Duckvalue::Text(__row.json.clone())
                    ]);
                }
                Ok(rows)
            }
            18usize => {
                let arg0 =
                    arg_witvalue_tgeompoint_sequence(&args, 0, "spatial_index_find_in_envelope")?;
                let arg1 = dv_f64(&args, 1, "spatial_index_find_in_envelope")?;
                let arg2 = dv_f64(&args, 2, "spatial_index_find_in_envelope")?;
                let arg3 = dv_f64(&args, 3, "spatial_index_find_in_envelope")?;
                let arg4 = dv_f64(&args, 4, "spatial_index_find_in_envelope")?;
                let __upstream = spatial_index_ops::spatial_index_find_in_envelope(
                    &arg0, arg1, arg2, arg3, arg4,
                );
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            19usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(
                    &args,
                    0,
                    "spatial_index_find_within_distance",
                )?;
                let arg1 = dv_f64(&args, 1, "spatial_index_find_within_distance")?;
                let arg2 = dv_f64(&args, 2, "spatial_index_find_within_distance")?;
                let arg3 = dv_f64(&args, 3, "spatial_index_find_within_distance")?;
                let __upstream =
                    spatial_index_ops::spatial_index_find_within_distance(&arg0, arg1, arg2, arg3);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            20usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "spatial_index_nearest")?;
                let arg1 = dv_f64(&args, 1, "spatial_index_nearest")?;
                let arg2 = dv_f64(&args, 2, "spatial_index_nearest")?;
                let __upstream = spatial_index_ops::spatial_index_nearest(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            21usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "stindex_nearest")?;
                let arg1 = dv_f64(&args, 1, "stindex_nearest")?;
                let arg2 = dv_f64(&args, 2, "stindex_nearest")?;
                let __upstream = spatial_index_ops::spatial_index_nearest(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            22usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "spatial_index_nearest_k")?;
                let arg1 = dv_f64(&args, 1, "spatial_index_nearest_k")?;
                let arg2 = dv_f64(&args, 2, "spatial_index_nearest_k")?;
                let arg3 = dv_i64(&args, 3, "spatial_index_nearest_k")? as i32;
                let __upstream =
                    spatial_index_ops::spatial_index_nearest_k(&arg0, arg1, arg2, arg3);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            23usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "stindex_nearest_k")?;
                let arg1 = dv_f64(&args, 1, "stindex_nearest_k")?;
                let arg2 = dv_f64(&args, 2, "stindex_nearest_k")?;
                let arg3 = dv_i64(&args, 3, "stindex_nearest_k")? as i32;
                let __upstream =
                    spatial_index_ops::spatial_index_nearest_k(&arg0, arg1, arg2, arg3);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.distance as f64)
                    ]);
                }
                Ok(rows)
            }
            24usize => Err(types::Duckerror::Unsupported(format!(
                "stindex_find_in_period: UDTF param #0 (list<record>) not wired"
            ))),
            25usize => Err(types::Duckerror::Unsupported(format!(
                "stindex_find_in_spatial: UDTF param #0 (list<record>) not wired"
            ))),
            26usize => Err(types::Duckerror::Unsupported(format!(
                "stindex_find_in_stbox: UDTF param #0 (list<record>) not wired"
            ))),
            27usize => {
                let arg0 = arg_witvalue_tcbuffer_sequence(&args, 0, "tcbuffer_to_rows")?;
                let __upstream = to_rows_ops::tcbuffer_to_rows(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.radius as f64)
                    ]);
                }
                Ok(rows)
            }
            28usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "temporal_index_find_after")?;
                let arg1 = dv_i64(&args, 1, "temporal_index_find_after")?;
                let __upstream = temporal_index_ops::temporal_index_find_after(&arg0, arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64)
                    ]);
                }
                Ok(rows)
            }
            29usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "temporal_index_find_at")?;
                let arg1 = dv_i64(&args, 1, "temporal_index_find_at")?;
                let __upstream = temporal_index_ops::temporal_index_find_at(&arg0, arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64)
                    ]);
                }
                Ok(rows)
            }
            30usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "temporal_index_find_before")?;
                let arg1 = dv_i64(&args, 1, "temporal_index_find_before")?;
                let __upstream = temporal_index_ops::temporal_index_find_before(&arg0, arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64)
                    ]);
                }
                Ok(rows)
            }
            31usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "temporal_index_find_in_range")?;
                let arg1 = dv_i64(&args, 1, "temporal_index_find_in_range")?;
                let arg2 = dv_i64(&args, 2, "temporal_index_find_in_range")?;
                let __upstream =
                    temporal_index_ops::temporal_index_find_in_range(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64)
                    ]);
                }
                Ok(rows)
            }
            32usize => {
                let arg0 =
                    arg_witvalue_tfloat_sequence(&args, 0, "temporal_index_find_overlapping")?;
                let arg1 = dv_i64(&args, 1, "temporal_index_find_overlapping")?;
                let arg2 = dv_i64(&args, 2, "temporal_index_find_overlapping")?;
                let __upstream =
                    temporal_index_ops::temporal_index_find_overlapping(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.instant_index as i64),
                        types::Duckvalue::Int64(__row.timestamp as i64)
                    ]);
                }
                Ok(rows)
            }
            33usize => {
                let arg0 = arg_witvalue_tbool_sequence(&args, 0, "temporal_join_bool")?;
                let arg1 = arg_witvalue_tbool_sequence(&args, 1, "temporal_join_bool")?;
                let __upstream = typed_join_ops::temporal_join_bool(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            34usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "temporal_join_float")?;
                let arg1 = arg_witvalue_tfloat_sequence(&args, 1, "temporal_join_float")?;
                let __upstream = join_ops::temporal_join_float(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.left as f64),
                        types::Duckvalue::Float64(__row.right as f64)
                    ]);
                }
                Ok(rows)
            }
            35usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "temporal_join_int")?;
                let arg1 = arg_witvalue_tint_sequence(&args, 1, "temporal_join_int")?;
                let __upstream = typed_join_ops::temporal_join_int(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Int64(__row.left as i64),
                        types::Duckvalue::Int64(__row.right as i64)
                    ]);
                }
                Ok(rows)
            }
            36usize => {
                let arg0 = arg_witvalue_ttext_sequence(&args, 0, "temporal_join_text")?;
                let arg1 = arg_witvalue_ttext_sequence(&args, 1, "temporal_join_text")?;
                let __upstream = typed_join_ops::temporal_join_text(&arg0, &arg1);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Text(__row.left.clone()),
                        types::Duckvalue::Text(__row.right.clone())
                    ]);
                }
                Ok(rows)
            }
            37usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_time_split")?;
                let arg1 = dv_i64(&args, 1, "tfloat_time_split")?;
                let arg2 = dv_i64(&args, 2, "tfloat_time_split")?;
                let __upstream = time_split_ops::tfloat_time_split(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.bucket_start as i64),
                        types::Duckvalue::Int64(__row.bucket_end as i64),
                        types::Duckvalue::Text(__row.sub_sequence_json.clone())
                    ]);
                }
                Ok(rows)
            }
            38usize => {
                let arg0 = arg_witvalue_tfloat_sequence(&args, 0, "tfloat_value_split")?;
                let arg1 = dv_f64(&args, 1, "tfloat_value_split")?;
                let arg2 = dv_f64(&args, 2, "tfloat_value_split")?;
                let __upstream = value_split_ops::tfloat_value_split(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Float64(__row.bucket_lower as f64),
                        types::Duckvalue::Float64(__row.bucket_upper as f64),
                        types::Duckvalue::Text(__row.sub_sequence_json.clone())
                    ]);
                }
                Ok(rows)
            }
            39usize => {
                let arg0 = arg_witvalue_tgeogpoint_sequence(&args, 0, "tgeogpoint_to_rows")?;
                let __upstream = to_rows_ops::tgeogpoint_to_rows(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.lon as f64),
                        types::Duckvalue::Float64(__row.lat as f64)
                    ]);
                }
                Ok(rows)
            }
            40usize => {
                let arg0 = arg_witvalue_tgeompoint3d_sequence(&args, 0, "tgeompoint3d_to_rows")?;
                let __upstream = to_rows_ops::tgeompoint3d_to_rows(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.z as f64)
                    ]);
                }
                Ok(rows)
            }
            41usize => {
                let arg0 = arg_witvalue_tgeompoint_sequence(&args, 0, "tgeompoint_to_rows")?;
                let __upstream = to_rows_ops::tgeompoint_to_rows(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64)
                    ]);
                }
                Ok(rows)
            }
            42usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_time_split")?;
                let arg1 = dv_i64(&args, 1, "tint_time_split")?;
                let arg2 = dv_i64(&args, 2, "tint_time_split")?;
                let __upstream = time_split_ops::tint_time_split(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.bucket_start as i64),
                        types::Duckvalue::Int64(__row.bucket_end as i64),
                        types::Duckvalue::Text(__row.sub_sequence_json.clone())
                    ]);
                }
                Ok(rows)
            }
            43usize => {
                let arg0 = arg_witvalue_tint_sequence(&args, 0, "tint_value_split")?;
                let arg1 = dv_i64(&args, 1, "tint_value_split")?;
                let arg2 = dv_i64(&args, 2, "tint_value_split")?;
                let __upstream = value_split_ops::tint_value_split(&arg0, arg1, arg2);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.bucket_lower as i64),
                        types::Duckvalue::Int64(__row.bucket_upper as i64),
                        types::Duckvalue::Text(__row.sub_sequence_json.clone())
                    ]);
                }
                Ok(rows)
            }
            44usize => {
                let arg0 = arg_witvalue_tpose_sequence(&args, 0, "tpose_to_rows")?;
                let __upstream = to_rows_ops::tpose_to_rows(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __row in __upstream.into_iter() {
                    rows.push(alloc::vec![
                        types::Duckvalue::Int64(__row.timestamp as i64),
                        types::Duckvalue::Float64(__row.x as f64),
                        types::Duckvalue::Float64(__row.y as f64),
                        types::Duckvalue::Float64(__row.orientation as f64)
                    ]);
                }
                Ok(rows)
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown table function arm index {}",
                arm_idx
            ))),
        }
    }
    fn call_aggregate(
        handle: u32,
        rows: types::Rowbatch,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        let arm_idx = aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown aggregate handle".into()))?;
        match arm_idx {
            0usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tbool_sequence(row, 0, "tbool_temporal_and")?);
                }
                let __r = temporal_aggregate_ops::tbool_temporal_and(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            1usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tbool_sequence(row, 0, "tand")?);
                }
                let __r = temporal_aggregate_ops::tbool_temporal_and(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            2usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tbool_sequence(row, 0, "tbool_temporal_or")?);
                }
                let __r = temporal_aggregate_ops::tbool_temporal_or(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            3usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tbool_sequence(row, 0, "tor")?);
                }
                let __r = temporal_aggregate_ops::tbool_temporal_or(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tbool_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            4usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tcbuffer_sequence(
                        row,
                        0,
                        "tcbuffer_temporal_count",
                    )?);
                }
                let __r = temporal_aggregate_ops::tcbuffer_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            5usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tfloat_temporal_avg")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_avg(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            6usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tavg")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_avg(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            7usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(
                        row,
                        0,
                        "tfloat_temporal_count",
                    )?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            8usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tcount")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            9usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tfloat_temporal_max")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_max(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            10usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tmax")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_max(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            11usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tfloat_temporal_min")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_min(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            12usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tmin")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_min(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            13usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tfloat_temporal_sum")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_sum(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            14usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tsum")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_sum(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            15usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tfloat_sequence(row, 0, "tfloat_total")?);
                }
                let __r = temporal_aggregate_ops::tfloat_temporal_sum(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tfloat_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            16usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeogpoint_sequence(
                        row,
                        0,
                        "tgeogpoint_temporal_count",
                    )?);
                }
                let __r = temporal_aggregate_ops::tgeogpoint_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            17usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_centroid_agg",
                    )?);
                }
                let __r = temporal_aggregate_ops::tgeompoint_temporal_centroid(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            18usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_num_convoys",
                    )?);
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let extras = extras.unwrap_or_default();
                let extra0 = dv_f64(&extras, 0, "tgeompoint_num_convoys")?;
                let extra1 = dv_i64(&extras, 1, "tgeompoint_num_convoys")?;
                let extra2 = dv_i64(&extras, 2, "tgeompoint_num_convoys")? as u32;
                let __r =
                    analytics_ops::tgeompoint_num_convoys(&upstream_vec, extra0, extra1, extra2);
                Ok(types::Duckvalue::Int64(__r as i64))
            }
            19usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_num_flocks",
                    )?);
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let extras = extras.unwrap_or_default();
                let extra0 = dv_f64(&extras, 0, "tgeompoint_num_flocks")?;
                let extra1 = dv_i64(&extras, 1, "tgeompoint_num_flocks")? as u32;
                let __r = analytics_ops::tgeompoint_num_flocks(&upstream_vec, extra0, extra1);
                Ok(types::Duckvalue::Int64(__r as i64))
            }
            20usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_num_meetings",
                    )?);
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let extras = extras.unwrap_or_default();
                let extra0 = dv_f64(&extras, 0, "tgeompoint_num_meetings")?;
                let extra1 = dv_i64(&extras, 1, "tgeompoint_num_meetings")?;
                let __r = analytics_ops::tgeompoint_num_meetings(&upstream_vec, extra0, extra1);
                Ok(types::Duckvalue::Int64(__r as i64))
            }
            21usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_st_extent",
                    )?);
                }
                let __r = temporal_aggregate_ops::tgeompoint_st_extent(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_stbox(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            22usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_temporal_centroid",
                    )?);
                }
                let __r = temporal_aggregate_ops::tgeompoint_temporal_centroid(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tgeompoint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            23usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tgeompoint_sequence(
                        row,
                        0,
                        "tgeompoint_temporal_count",
                    )?);
                }
                let __r = temporal_aggregate_ops::tgeompoint_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            24usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_count_agg")?);
                }
                let __r = temporal_aggregate_ops::tint_count_aggregate(&upstream_vec);
                Ok(types::Duckvalue::Int64(__r as i64))
            }
            25usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_max_agg")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_max(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            26usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_min_agg")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_min(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            27usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_sum_agg")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_sum(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            28usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_temporal_max")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_max(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            29usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_temporal_min")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_min(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            30usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tint_sequence(row, 0, "tint_temporal_sum")?);
                }
                let __r = temporal_aggregate_ops::tint_temporal_sum(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            31usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tnpoint_sequence(
                        row,
                        0,
                        "tnpoint_temporal_count",
                    )?);
                }
                let __r = temporal_aggregate_ops::tnpoint_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            32usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_tpose_sequence(row, 0, "tpose_temporal_count")?);
                }
                let __r = temporal_aggregate_ops::tpose_temporal_count(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_tint_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            33usize => {
                let mut upstream_vec = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    upstream_vec.push(arg_witvalue_ttext_sequence(row, 0, "ttext_concat_agg")?);
                }
                let __r = temporal_aggregate_ops::ttext_concat_aggregate(&upstream_vec);
                match __r {
                    Some(__rec) => ret_to_witvalue_ttext_sequence(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown aggregate arm index {}",
                arm_idx
            ))),
        }
    }
    fn call_pragma(
        _handle: u32,
        _args: Vec<types::Duckvalue>,
    ) -> Result<Option<types::Duckvalue>, types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "mobilitydb: pragmas not wired (Step 4 scalar-first cut)"
        )))
    }
    fn call_cast(
        handle: u32,
        value: types::Duckvalue,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        <Self as callback_dispatch::Guest>::call_scalar(
            handle,
            alloc::vec![value],
            types::Invokeinfo {
                rowindex: None,
                iswindow: false,
            },
        )
    }
}

fn register_scalars() -> Result<(), types::Duckerror> {
    let capability = runtime::get_capability(types::Capabilitykind::Scalar).ok_or_else(|| {
        types::Duckerror::Internal("host did not expose scalar capability".into())
    })?;
    let registry = match capability {
        runtime::Capability::Scalar(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "scalar capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("angular_diff (sqlink-shim-codegen)".into()),
            tags: vec!["angular_diff".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "angular_diff",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("average_frechet_distance (sqlink-shim-codegen)".into()),
            tags: vec!["average_frechet_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "average_frechet_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bearing (sqlink-shim-codegen)".into()),
            tags: vec!["bearing".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bearing",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_as_of (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_as_of".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_as_of",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_count_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_count_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_count_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_count_superseded (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_count_superseded".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_count_superseded",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "bitemporal_bool_ever_valid_during_count (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["bitemporal_bool_ever_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_ever_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_from_ewkt",
            &args,
            &types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_history_at_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_history_at_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_history_at_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_len (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_transaction_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_transaction_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_transaction_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_at (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_valid_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_at",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_during_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-bool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_bool_valid_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_as_of (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_as_of".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_as_of",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_count_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_count_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_count_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_count_superseded (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_count_superseded".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_count_superseded",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "bitemporal_float_ever_valid_during_count (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["bitemporal_float_ever_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_ever_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_from_ewkt",
            &args,
            &types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_history_at_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_history_at_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_history_at_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_len (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_transaction_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_transaction_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_transaction_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_at (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_valid_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_during_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-float-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_float_valid_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_as_of (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_as_of".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_as_of",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_count_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_count_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_count_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_count_superseded (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_count_superseded".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_count_superseded",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "bitemporal_int_ever_valid_during_count (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["bitemporal_int_ever_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_ever_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_from_ewkt",
            &args,
            &types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_history_at_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_history_at_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_history_at_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_len (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_transaction_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_transaction_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_transaction_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_at (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_valid_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_at",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_during_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-int-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_int_valid_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_as_of (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_as_of".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_as_of",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_count_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_count_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_count_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_count_superseded (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_count_superseded".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_count_superseded",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 45usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_current (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_current".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_current",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 46usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "bitemporal_text_ever_valid_during_count (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["bitemporal_text_ever_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_ever_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 47usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_from_ewkt",
            &args,
            &types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 48usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_history_at_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_history_at_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_history_at_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 49usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_len (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 50usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 51usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_transaction_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_transaction_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_transaction_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 52usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_at (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_valid_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_at",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 53usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_during_count (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_valid_during_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_during_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 54usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("bitemporal-text-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_extent (sqlink-shim-codegen)".into()),
            tags: vec!["bitemporal_text_valid_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_extent",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 55usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 56usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_contains (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 57usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 58usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_expand (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_expand",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 59usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_intersection",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 60usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_make (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_make",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 61usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 62usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_shift (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_shift",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 63usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_span_union (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_union",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 64usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("date-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("date_span_width (sqlink-shim-codegen)".into()),
            tags: vec!["date_span_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_span_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 65usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 66usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_intersection",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 67usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_minus",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 68usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 69usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 70usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_union",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 71usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["date_spanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 72usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 73usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_gaps (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_gaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_gaps",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 74usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_intersection",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 75usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_len (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 76usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_make (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_make",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 77usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_minus",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 78usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_nth (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_nth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_nth",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 79usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_shift (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_shift",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 80usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_successor (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_successor".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_successor",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 81usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_to_span (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_to_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_to_span",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 82usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("dateset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 83usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dateset_union (sqlink-shim-codegen)".into()),
            tags: vec!["dateset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dateset_union",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 84usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 85usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_contains (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 86usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 87usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_expand (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_expand",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 88usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespan_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_from_text",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 89usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_intersection",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 90usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("date-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("datespan_lower (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_lower".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_lower",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 91usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_make (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_make",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 92usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 93usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_shift (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_shift",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 94usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("date-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("datespan_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 95usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("date-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespan_union (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_union",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 96usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("date-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("datespan_upper (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_upper".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_upper",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 97usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("date-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("datespan_width (sqlink-shim-codegen)".into()),
            tags: vec!["datespan_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespan_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 98usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 99usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_from_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_intersection",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_lower (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_lower".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_lower",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_make (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_make",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_minus",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_union",
            &args,
            &types::Logicaltype::Complex("date-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_upper (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_upper".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_upper",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("datespanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["datespanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("distance (sqlink-shim-codegen)".into()),
            tags: vec!["distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dtw_distance (sqlink-shim-codegen)".into()),
            tags: vec!["dtw_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dtw_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("dtw_distance_normalized (sqlink-shim-codegen)".into()),
            tags: vec!["dtw_distance_normalized".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "dtw_distance_normalized",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("edr_distance (sqlink-shim-codegen)".into()),
            tags: vec!["edr_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "edr_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_contains (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_expand (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_expand",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_intersection",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_make (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_make",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_shift (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_shift",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_span_union (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_union",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("float_span_width (sqlink-shim-codegen)".into()),
            tags: vec!["float_span_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_span_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_intersection",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_minus",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_union",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["float_spanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatset_gaps (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_gaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_gaps",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_intersection",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_is_disjoint (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_is_disjoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_is_disjoint",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_is_subset (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_is_subset".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_is_subset",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatset_len (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatset_make (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_make",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_minus",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_nth (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_nth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_nth",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_scale (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_scale",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_shift (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_shift",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatset_to_span (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_to_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_to_span",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatset_union (sqlink-shim-codegen)".into()),
            tags: vec!["floatset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatset_union",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_bucket_list (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_bucket_list".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_bucket_list",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_contains (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_expand (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_expand",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_from_text",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_intersection",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_lower (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_lower".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_lower",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_make (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_make",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_midpoint (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_midpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_midpoint",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_minus (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_minus",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_scale (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_scale",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_shift (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_shift",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("float-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_union (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_union",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_upper (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_upper".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_upper",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("float-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspan_width (sqlink-shim-codegen)".into()),
            tags: vec!["floatspan_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_from_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_intersection",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_minus",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_union",
            &args,
            &types::Logicaltype::Complex("float-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["floatspanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("frechet_distance (sqlink-shim-codegen)".into()),
            tags: vec!["frechet_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "frechet_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("hausdorff_distance (sqlink-shim-codegen)".into()),
            tags: vec!["hausdorff_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "hausdorff_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_contains (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_expand (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_expand",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_intersection",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_make (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_make",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_shift (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_shift",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_span_union (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_union",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("int_span_width (sqlink-shim-codegen)".into()),
            tags: vec!["int_span_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_span_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_intersection",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_minus",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_union",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["int_spanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["intset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intset_gaps (sqlink-shim-codegen)".into()),
            tags: vec!["intset_gaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_gaps",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["intset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_intersection",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_is_disjoint (sqlink-shim-codegen)".into()),
            tags: vec!["intset_is_disjoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_is_disjoint",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_is_subset (sqlink-shim-codegen)".into()),
            tags: vec!["intset_is_subset".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_is_subset",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intset_len (sqlink-shim-codegen)".into()),
            tags: vec!["intset_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intset_make (sqlink-shim-codegen)".into()),
            tags: vec!["intset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_make",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["intset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_minus",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_nth (sqlink-shim-codegen)".into()),
            tags: vec!["intset_nth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_nth",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_scale (sqlink-shim-codegen)".into()),
            tags: vec!["intset_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_scale",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_shift (sqlink-shim-codegen)".into()),
            tags: vec!["intset_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_shift",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intset_to_span (sqlink-shim-codegen)".into()),
            tags: vec!["intset_to_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_to_span",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["intset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intset_union (sqlink-shim-codegen)".into()),
            tags: vec!["intset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intset_union",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_bucket_list (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_bucket_list".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_bucket_list",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_contains (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_expand (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_expand",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intspan_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_from_text",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_intersection",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("intspan_lower (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_lower".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_lower",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_make (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_make",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_minus (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_minus",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_scale (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_scale",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_shift (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_shift",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("intspan_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("int-span".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspan_union (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_union",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("intspan_upper (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_upper".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_upper",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("intspan_width (sqlink-shim-codegen)".into()),
            tags: vec!["intspan_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspan_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intspanset_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_from_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 224usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_intersection",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 225usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_minus",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 226usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intspanset_num_spans (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_num_spans".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_num_spans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 227usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 228usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intspanset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 229usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_union (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_union",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 230usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("intspanset_width (sqlink-shim-codegen)".into()),
            tags: vec!["intspanset_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 231usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("lcss_distance (sqlink-shim-codegen)".into()),
            tags: vec!["lcss_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "lcss_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 232usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("lcss_similarity (sqlink-shim-codegen)".into()),
            tags: vec!["lcss_similarity".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "lcss_similarity",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 233usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("network_distance_via_edges (sqlink-shim-codegen)".into()),
            tags: vec!["network_distance_via_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "network_distance_via_edges",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 234usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("network_distance_via_graph (sqlink-shim-codegen)".into()),
            tags: vec!["network_distance_via_graph".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "network_distance_via_graph",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 235usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("network_graph_build_from_edges (sqlink-shim-codegen)".into()),
            tags: vec!["network_graph_build_from_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "network_graph_build_from_edges",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 236usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("network_shortest_path_via_edges (sqlink-shim-codegen)".into()),
            tags: vec!["network_shortest_path_via_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "network_shortest_path_via_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 237usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("network_shortest_path_via_graph (sqlink-shim-codegen)".into()),
            tags: vec!["network_shortest_path_via_graph".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "network_shortest_path_via_graph",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 238usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("npoint".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("npoint".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("npoint_distance (sqlink-shim-codegen)".into()),
            tags: vec!["npoint_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "npoint_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 239usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("npoint_new (sqlink-shim-codegen)".into()),
            tags: vec!["npoint_new".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "npoint_new",
            &args,
            &types::Logicaltype::Complex("npoint".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 240usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("npoint".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("npoint".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("npoint_same_route (sqlink-shim-codegen)".into()),
            tags: vec!["npoint_same_route".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "npoint_same_route",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 241usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("nsegment".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("npoint".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("nsegment_contains (sqlink-shim-codegen)".into()),
            tags: vec!["nsegment_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nsegment_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 242usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("nsegment".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("nsegment_length (sqlink-shim-codegen)".into()),
            tags: vec!["nsegment_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nsegment_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 243usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("nsegment".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("nsegment_midpoint (sqlink-shim-codegen)".into()),
            tags: vec!["nsegment_midpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nsegment_midpoint",
            &args,
            &types::Logicaltype::Complex("npoint".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 244usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("nsegment_new (sqlink-shim-codegen)".into()),
            tags: vec!["nsegment_new".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nsegment_new",
            &args,
            &types::Logicaltype::Complex("nsegment".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 245usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("nsegment".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("nsegment".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("nsegment_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["nsegment_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nsegment_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 246usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 247usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_area (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 248usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_center_time (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_center_time".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_center_time",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 249usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_center_x (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_center_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_center_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 250usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_center_y (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_center_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_center_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 251usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_contained_by (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_contained_by".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_contained_by",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 252usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_contains (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 253usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_contains_point (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_contains_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_contains_point",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 254usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_duration (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_duration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_duration",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 255usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_expand_spatial (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_expand_spatial".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_expand_spatial",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 256usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_expand_temporal (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_expand_temporal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_expand_temporal",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 257usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_from_spatial (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_from_spatial".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_from_spatial",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 258usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("time-period".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_from_temporal (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_from_temporal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_from_temporal",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 259usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_from_text",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 260usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_height (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_height".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_height",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 261usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_intersection",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 262usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_make (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_make",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 263usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 264usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_same (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_same".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_same",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 265usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_scale_spatial (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_scale_spatial".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_scale_spatial",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 266usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_scale_temporal (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_scale_temporal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_scale_temporal",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 267usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_shift_spatial (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_shift_spatial".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_shift_spatial",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 268usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_shift_temporal (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_shift_temporal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_shift_temporal",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 269usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_spatial_distance (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_spatial_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_spatial_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 270usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_spatially_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_spatially_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_spatially_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 271usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_temporal_distance (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_temporal_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_temporal_distance",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 272usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_temporally_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_temporally_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_temporally_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 273usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_tmax (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_tmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_tmax",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 274usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_tmin (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_tmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_tmin",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 275usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 276usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_transform (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_transform".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_transform",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 277usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stbox_union (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_union",
            &args,
            &types::Logicaltype::Complex("stbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 278usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_width (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 279usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_xmax (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_xmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_xmax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 280usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_xmin (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_xmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_xmin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 281usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_ymax (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_ymax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_ymax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 282usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("stbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox_ymin (sqlink-shim-codegen)".into()),
            tags: vec!["stbox_ymin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox_ymin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 283usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("stindex_count_in_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["stindex_count_in_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stindex_count_in_stbox",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 284usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_always_false (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_always_false".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_always_false",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 285usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_always_true (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_always_true".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_always_true",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 286usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_and (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_and".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_and",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 287usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_false (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_at_false".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_false",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 288usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_period",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 289usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_true (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_at_true".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_true",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 290usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_value (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_at_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_value",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 291usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_changes (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_count_changes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_changes",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 292usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_false (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_count_false".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_false",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 293usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_true (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_count_true".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_true",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 294usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_duration (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_duration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_duration",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 295usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_end_timestamp (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_end_timestamp".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_end_timestamp",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 296usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_end_value (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_end_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_end_value",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 297usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_ever_false (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_ever_false".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_ever_false",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 298usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_ever_true (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_ever_true".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_ever_true",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 299usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_from_csv (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_from_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_from_csv",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 300usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_minus_period (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_minus_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_minus_period",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 301usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_minus_value (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_minus_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_minus_value",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 302usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_not (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_not".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_not",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 303usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 304usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_or (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_or".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_or",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 305usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_start_timestamp (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_start_timestamp".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_start_timestamp",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 306usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_start_value (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_start_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_start_value",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 307usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 308usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_to_csv (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_to_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_to_csv",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 309usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbool-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbool_to_json (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_to_json".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_to_json",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 310usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tbool_value_at (sqlink-shim-codegen)".into()),
            tags: vec!["tbool_value_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbool_value_at",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 311usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tbox_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["tbox_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbox_from_text",
            &args,
            &types::Logicaltype::Complex("tbox".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 312usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tbox".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tbox_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["tbox_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tbox_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 313usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_always_contains_point (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_always_contains_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_always_contains_point",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 314usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_area_at (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_area_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_area_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 315usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_avg_area (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_avg_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_avg_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 316usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_avg_radius (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_avg_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_avg_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 317usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_center_path_length (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_center_path_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_center_path_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 318usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_contains_point_at (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_contains_point_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_contains_point_at",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 319usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_ever_contains_point (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_ever_contains_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_ever_contains_point",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_from_ewkt",
            &args,
            &types::Logicaltype::Complex("tcbuffer-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 321usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_get_area (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_get_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_get_area",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 322usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_get_center_x (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_get_center_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_get_center_x",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 323usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_get_center_y (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_get_center_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_get_center_y",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 324usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_get_radius (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_get_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_get_radius",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 325usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_max_radius (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_max_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_max_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 326usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_min_radius (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_min_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_min_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 327usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 328usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_radius_at (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_radius_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_radius_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 329usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_swept_area (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_swept_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_swept_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 330usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 331usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 332usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_to_tgeompoint (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_to_tgeompoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_to_tgeompoint",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 333usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_total_area (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_total_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_total_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 334usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_x_at (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_x_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_x_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 335usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tcbuffer_y_at (sqlink-shim-codegen)".into()),
            tags: vec!["tcbuffer_y_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tcbuffer_y_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 336usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_contains (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 337usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_during (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_during".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_during",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 338usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_finishes (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_finishes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_finishes",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 339usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_follows (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_follows".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_follows",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 340usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_meets (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_meets".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_meets",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 341usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 342usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_precedes (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_precedes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_precedes",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 343usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("temporal_starts (sqlink-shim-codegen)".into()),
            tags: vec!["temporal_starts".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "temporal_starts",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 344usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["textset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 345usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["textset_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_from_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 346usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["textset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_intersection",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 347usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_disjoint (sqlink-shim-codegen)".into()),
            tags: vec!["textset_is_disjoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_disjoint",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 348usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_subset (sqlink-shim-codegen)".into()),
            tags: vec!["textset_is_subset".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_subset",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 349usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_superset (sqlink-shim-codegen)".into()),
            tags: vec!["textset_is_superset".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_superset",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 350usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_len (sqlink-shim-codegen)".into()),
            tags: vec!["textset_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 351usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_make (sqlink-shim-codegen)".into()),
            tags: vec!["textset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_make",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 352usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_max (sqlink-shim-codegen)".into()),
            tags: vec!["textset_max".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_max",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 353usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_min (sqlink-shim-codegen)".into()),
            tags: vec!["textset_min".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_min",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 354usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["textset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_minus",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 355usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_symdiff (sqlink-shim-codegen)".into()),
            tags: vec!["textset_symdiff".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_symdiff",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 356usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["textset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 357usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("textset_union (sqlink-shim-codegen)".into()),
            tags: vec!["textset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_union",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 358usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("textset_values (sqlink-shim-codegen)".into()),
            tags: vec!["textset_values".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "textset_values",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 359usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_abs (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_abs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_abs",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 360usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_add (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_add".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_add",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 361usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_add_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_add_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_add_scalar",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 362usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 363usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_eq_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_eq_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_eq_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 364usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_gt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 365usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_gt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_gt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_gt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 366usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_lt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 367usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_lt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_always_lt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_lt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 368usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_max (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_at_max".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_max",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 369usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_min (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_at_min".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_min",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 370usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_period",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 371usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_value (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_at_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_value",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 372usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_cos (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_cos".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_cos",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 373usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_derivative (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_derivative".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_derivative",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 374usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_derivative_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_derivative_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_derivative_wit",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 375usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_periodicity (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_detect_periodicity".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_periodicity",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 376usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_div (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_div".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_div",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 377usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_div_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_div_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_div_scalar",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 378usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 379usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_eq_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_eq_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_eq_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 380usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_gt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 381usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_gt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_gt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_gt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 382usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_lt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 383usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_lt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ever_lt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_lt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 384usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_exp (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_exp".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_exp",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 385usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_integral (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_integral".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_integral",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 386usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ln (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_ln".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ln",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 387usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_log10 (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_log10".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_log10",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 388usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_max_value (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_max_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_max_value",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 389usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_max_value_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_max_value_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_max_value_wit",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 390usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_min_value (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_min_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_min_value",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 391usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_min_value_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_min_value_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_min_value_wit",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 392usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_minus_period (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_minus_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_minus_period",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 393usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_minus_value (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_minus_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_minus_value",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 394usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mul (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_mul".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mul",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 395usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mul_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_mul_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mul_scalar",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 396usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_negate (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_negate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_negate",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 397usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 398usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_num_instants_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_num_instants_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_num_instants_wit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 399usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_pct_change (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_pct_change".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_pct_change",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 400usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_pow (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_pow".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_pow",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 401usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sample_entropy (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_sample_entropy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sample_entropy",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 402usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sin (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_sin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sin",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 403usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sqrt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_sqrt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sqrt",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 404usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sub (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_sub".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sub",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 405usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sub_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_sub_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sub_scalar",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 406usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_tan (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_tan".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_tan",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 407usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_correlation (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_correlation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_correlation",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 408usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_covariance (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_covariance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_covariance",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 409usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_eq",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 410usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_ge (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_ge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_ge",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 411usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_gt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 412usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_le (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_le".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_le",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 413usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_lt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 414usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_ne (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_temporal_ne".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_ne",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 415usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 416usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_json (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_to_json".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_json",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 417usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twavg (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twavg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twavg",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 418usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twcount (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twcount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twcount",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 419usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twmax (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twmax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 420usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twmin (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twmin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 421usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twstddev (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twstddev".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twstddev",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 422usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twsum (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twsum".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twsum",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 423usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twvariance (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_twvariance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twvariance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 424usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_at (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_value_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 425usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_eq",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 426usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_ge (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_ge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_ge",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 427usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_gt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 428usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_le (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_le".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_le",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 429usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_lt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 430usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_ne (sqlink-shim-codegen)".into()),
            tags: vec!["tfloat_when_ne".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_ne",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 431usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "tgeogpoint_always_dwithin_point_geodetic (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["tgeogpoint_always_dwithin_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_always_dwithin_point_geodetic",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 432usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_at_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_at_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_at_stbox",
            &args,
            &types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 433usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_avg_speed_geodetic (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_avg_speed_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_avg_speed_geodetic",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 434usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_azimuth_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_azimuth_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_azimuth_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 435usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_distance_to_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_distance_to_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_distance_to_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 436usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_distance_to_point_seq (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_distance_to_point_seq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_distance_to_point_seq",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 437usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_dwithin_point_geodetic (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_dwithin_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_dwithin_point_geodetic",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 438usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_lat_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_lat_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_lat_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 439usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_length_geodetic (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_length_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_length_geodetic",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 440usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_lon_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_lon_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_lon_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 441usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "tgeogpoint_max_distance_to_point_geodetic (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["tgeogpoint_max_distance_to_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_max_distance_to_point_geodetic",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 442usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_min_distance (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_min_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_min_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 443usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some(
                "tgeogpoint_min_distance_to_point_geodetic (sqlink-shim-codegen)".into(),
            ),
            tags: vec!["tgeogpoint_min_distance_to_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_min_distance_to_point_geodetic",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 444usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_minus_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_minus_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_minus_stbox",
            &args,
            &types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 445usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 446usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_num_stops (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_num_stops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_num_stops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 447usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_num_turns (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_num_turns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_num_turns",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 448usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_speed_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_speed_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_speed_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 449usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_tdisjoint_point_geodetic (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_tdisjoint_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_tdisjoint_point_geodetic",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 450usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 451usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeogpoint_tintersects_point_geodetic (sqlink-shim-codegen)".into()),
            tags: vec!["tgeogpoint_tintersects_point_geodetic".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeogpoint_tintersects_point_geodetic",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 452usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_at_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_at_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_at_point",
            &args,
            &types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 453usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_avg_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_avg_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_avg_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 454usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_azimuth (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_azimuth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_azimuth",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 455usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_cumulative_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_cumulative_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_cumulative_length",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 456usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_distance_to_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_distance_to_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_distance_to_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 457usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_dtw_distance (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_dtw_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_dtw_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 458usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_frechet_distance (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_frechet_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_frechet_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 459usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_from_csv (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_from_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_from_csv",
            &args,
            &types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_from_ewkt",
            &args,
            &types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 461usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_hausdorff_distance (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_hausdorff_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_hausdorff_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 462usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 463usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_minus_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_minus_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_minus_point",
            &args,
            &types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 464usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 465usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_pitch (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_pitch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_pitch",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 466usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_planar_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_planar_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_planar_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 467usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 468usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_to_csv (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_to_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_to_csv",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 469usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 470usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_total_climb (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_total_climb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_total_climb",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 471usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_total_descent (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_total_descent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_total_descent",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 472usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_value_at_x (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_value_at_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_value_at_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 473usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_value_at_y (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_value_at_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_value_at_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 474usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_value_at_z (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_value_at_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_value_at_z",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 475usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint3d_vertical_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint3d_vertical_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint3d_vertical_speed",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 476usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_acceleration_count (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_acceleration_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_acceleration_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 477usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_acceleration_event_count (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_acceleration_event_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_acceleration_event_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 478usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_acceleration_events (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_acceleration_events".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_acceleration_events",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 479usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_angular_difference (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_angular_difference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_angular_difference",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 480usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_at_period",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 481usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_at_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_at_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_at_point",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 482usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_at_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_at_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_at_stbox",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 483usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_avg_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_avg_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_avg_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 484usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_azimuth (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_azimuth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_azimuth",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 485usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_bearing (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_bearing".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_bearing",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 486usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_bounding_box (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_bounding_box".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_bounding_box",
            &args,
            &types::Logicaltype::Complex("bounding-box".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 487usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_cluster_count_dbscan (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_cluster_count_dbscan".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_cluster_count_dbscan",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 488usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_cumulative_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_cumulative_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_cumulative_length",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 489usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_detect_loops (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_detect_loops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_detect_loops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 490usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_detect_stops (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_detect_stops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_detect_stops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 491usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_detect_turns (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_detect_turns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_detect_turns",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 492usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_detect_u_turns (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_detect_u_turns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_detect_u_turns",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 493usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_direction_stats (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_direction_stats".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_direction_stats",
            &args,
            &types::Logicaltype::Complex("direction-stats".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 494usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_distance_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_distance_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_distance_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 495usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("geom-point".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_distance_to_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_distance_to_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_distance_to_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 496usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_from_web_mercator (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_from_web_mercator".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_from_web_mercator",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 497usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_full_analysis (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_full_analysis".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_full_analysis",
            &args,
            &types::Logicaltype::Complex("trajectory-analysis".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 498usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_geodetic_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_geodetic_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_geodetic_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 499usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_geodetic_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_geodetic_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_geodetic_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 500usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_length (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 501usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_max_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_max_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_max_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 502usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_min_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_min_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_min_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 503usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_minus_period (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_minus_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_minus_period",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 504usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_minus_point (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_minus_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_minus_point",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 505usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_minus_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_minus_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_minus_stbox",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 506usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_movement_summary (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_movement_summary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_movement_summary",
            &args,
            &types::Logicaltype::Complex("movement-summary".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 507usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_nearest_approach_distance (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_nearest_approach_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_nearest_approach_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 508usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 509usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_num_loops (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_num_loops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_num_loops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 510usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_num_stops (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_num_stops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_num_stops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 511usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_num_turns (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_num_turns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_num_turns",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 512usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_num_u_turns (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_num_u_turns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_num_u_turns",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 513usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_overlaps_stbox (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_overlaps_stbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_overlaps_stbox",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 514usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_quality_score (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_quality_score".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_quality_score",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 515usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_simplify (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_simplify".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_simplify",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 516usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_speed_stats (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_speed_stats".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_speed_stats",
            &args,
            &types::Logicaltype::Complex("speed-stats".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 517usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_speeds (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_speeds".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_speeds",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 518usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_srid (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 519usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_to_geojson (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_to_geojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_to_geojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 520usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_to_geojson_feature (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_to_geojson_feature".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_to_geojson_feature",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 521usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_to_web_mercator (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_to_web_mercator".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_to_web_mercator",
            &args,
            &types::Logicaltype::Complex("tgeompoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 522usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_tortuosity (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_tortuosity".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_tortuosity",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 523usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tgeompoint_value_at (sqlink-shim-codegen)".into()),
            tags: vec!["tgeompoint_value_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tgeompoint_value_at",
            &args,
            &types::Logicaltype::Complex("geom-point".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 524usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_abs (sqlink-shim-codegen)".into()),
            tags: vec!["tint_abs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_abs",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 525usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_add (sqlink-shim-codegen)".into()),
            tags: vec!["tint_add".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_add",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 526usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_add_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_add_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_add_scalar",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 527usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 528usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_eq_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_eq_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_eq_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 529usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_gt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 530usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_gt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_gt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_gt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 531usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_lt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 532usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_lt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_always_lt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_lt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 533usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_at_max (sqlink-shim-codegen)".into()),
            tags: vec!["tint_at_max".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_max",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 534usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_at_min (sqlink-shim-codegen)".into()),
            tags: vec!["tint_at_min".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_min",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 535usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["tint_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_period",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 536usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_value (sqlink-shim-codegen)".into()),
            tags: vec!["tint_at_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_value",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 537usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_diff (sqlink-shim-codegen)".into()),
            tags: vec!["tint_diff".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_diff",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 538usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_div (sqlink-shim-codegen)".into()),
            tags: vec!["tint_div".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_div",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 539usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_div_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_div_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_div_scalar",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 540usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 541usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_eq_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_eq_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_eq_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 542usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_gt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 543usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_gt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_gt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_gt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 544usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_lt",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 545usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_lt_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_ever_lt_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_lt_scalar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 546usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_exponential_smooth (sqlink-shim-codegen)".into()),
            tags: vec!["tint_exponential_smooth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_exponential_smooth",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 547usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_integral (sqlink-shim-codegen)".into()),
            tags: vec!["tint_integral".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_integral",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 548usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_max_value_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_scalar",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 549usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tint_max_value_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_wit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 550usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_min_value_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_scalar",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 551usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tint_min_value_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_wit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 552usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_period (sqlink-shim-codegen)".into()),
            tags: vec!["tint_minus_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_period",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 553usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_value (sqlink-shim-codegen)".into()),
            tags: vec!["tint_minus_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_value",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 554usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_mod_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_mod_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_mod_scalar",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 555usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_moving_average (sqlink-shim-codegen)".into()),
            tags: vec!["tint_moving_average".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_moving_average",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 556usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_mul (sqlink-shim-codegen)".into()),
            tags: vec!["tint_mul".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_mul",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 557usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_mul_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_mul_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_mul_scalar",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 558usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_negate (sqlink-shim-codegen)".into()),
            tags: vec!["tint_negate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_negate",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 559usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tint_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 560usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_num_instants_wit (sqlink-shim-codegen)".into()),
            tags: vec!["tint_num_instants_wit".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_instants_wit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 561usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_sequence_to_tfloat (sqlink-shim-codegen)".into()),
            tags: vec!["tint_sequence_to_tfloat".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequence_to_tfloat",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 562usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_sub (sqlink-shim-codegen)".into()),
            tags: vec!["tint_sub".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_sub",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 563usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_sub_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["tint_sub_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_sub_scalar",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 564usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_eq",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 565usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_ge (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_ge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_ge",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 566usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_gt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 567usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_le (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_le".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_le",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 568usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_lt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 569usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_ne (sqlink-shim-codegen)".into()),
            tags: vec!["tint_temporal_ne".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_ne",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 570usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tint_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 571usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_to_json (sqlink-shim-codegen)".into()),
            tags: vec!["tint_to_json".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_json",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 572usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twavg (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twavg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twavg",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 573usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twcount (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twcount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twcount",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 574usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twmax (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twmax",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 575usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twmin (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twmin",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 576usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twstddev (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twstddev".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twstddev",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 577usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twsum (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twsum".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twsum",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 578usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tint_twvariance (sqlink-shim-codegen)".into()),
            tags: vec!["tint_twvariance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_twvariance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 579usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_at (sqlink-shim-codegen)".into()),
            tags: vec!["tint_value_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_at",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 580usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_eq (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_eq",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 581usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_ge (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_ge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_ge",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 582usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_gt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_gt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_gt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 583usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_le (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_le".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_le",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 584usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_lt (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_lt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_lt",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 585usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_ne (sqlink-shim-codegen)".into()),
            tags: vec!["tint_when_ne".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_ne",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 586usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_at_period",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 587usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_at_position_range (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_at_position_range".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_at_position_range",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 588usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_avg_network_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_avg_network_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_avg_network_speed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 589usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_cumulative_length (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_cumulative_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_cumulative_length",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 590usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_end_position (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_end_position".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_end_position",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_from_ewkt",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 592usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_is_backward (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_is_backward".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_is_backward",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 593usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_is_forward (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_is_forward".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_is_forward",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 594usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_is_single_route (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_is_single_route".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_is_single_route",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 595usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_max_position (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_max_position".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_max_position",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 596usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_min_position (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_min_position".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_min_position",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 597usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_network_length (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_network_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_network_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 598usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_network_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_network_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_network_speed",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 599usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 600usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_num_route_changes (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_num_route_changes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_num_route_changes",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 601usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_num_stops (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_num_stops".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_num_stops",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 602usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_position_at (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_position_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_position_at",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 603usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_positions (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_positions".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_positions",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 604usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_route_id (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_route_id".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_route_id",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 605usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_scale_time (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_scale_time".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_scale_time",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 606usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_shift_time (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_shift_time".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_shift_time",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 607usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_simplify (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_simplify".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_simplify",
            &args,
            &types::Logicaltype::Complex("tnpoint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 608usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_start_position (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_start_position".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_start_position",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 609usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_stop_duration (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_stop_duration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_stop_duration",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 610usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 611usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tnpoint-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tnpoint_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tnpoint_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tnpoint_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 612usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_angular_acceleration (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_angular_acceleration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_angular_acceleration",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 613usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_angular_distance_to_pose (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_angular_distance_to_pose".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_angular_distance_to_pose",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 614usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_angular_velocity (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_angular_velocity".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_angular_velocity",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 615usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_avg_orientation (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_avg_orientation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_avg_orientation",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 616usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_compute_heading_from_movement (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_compute_heading_from_movement".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_compute_heading_from_movement",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 617usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_curvature (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_curvature".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_curvature",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 618usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_distance_to_point (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_distance_to_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_distance_to_point",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 619usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_distance_to_pose (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_distance_to_pose".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_distance_to_pose",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_from_ewkt",
            &args,
            &types::Logicaltype::Complex("tpose-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 621usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_get_orientation (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_get_orientation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_get_orientation",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 622usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_get_x (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_get_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_get_x",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 623usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_get_y (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_get_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_get_y",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 624usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_linear_acceleration (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_linear_acceleration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_linear_acceleration",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 625usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_linear_speed (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_linear_speed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_linear_speed",
            &args,
            &types::Logicaltype::Complex("tfloat-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 626usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_nearest_approach (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_nearest_approach".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_nearest_approach",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 627usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_nearest_approach_point (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_nearest_approach_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_nearest_approach_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 628usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 629usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_path_length (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_path_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_path_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 630usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_pose_at_orientation (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_pose_at_orientation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_pose_at_orientation",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 631usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_pose_at_x (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_pose_at_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_pose_at_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 632usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tpose-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tpose_pose_at_y (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_pose_at_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_pose_at_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 633usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 634usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_to_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_to_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_to_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 635usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tpose_total_rotation (sqlink-shim-codegen)".into()),
            tags: vec!["tpose_total_rotation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tpose_total_rotation",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 636usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_contains (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 637usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_intersection",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 638usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzset_len (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_len".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_len",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 639usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzset_make (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_make",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 640usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_minus (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_minus",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 641usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_nth (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_nth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_nth",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 642usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 643usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_shift (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_shift",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 644usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzset_successor (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_successor".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_successor",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 645usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 646usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_union (sqlink-shim-codegen)".into()),
            tags: vec!["tstzset_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_union",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 647usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_adjacent (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_adjacent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_adjacent",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 648usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_contains (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 649usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_contains_span (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_contains_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_contains_span",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 650usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tstzspan".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_duration (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_duration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_duration",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 651usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tstzspan".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_end (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_end".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_end",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 652usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_expand (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_expand",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 653usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_from_text",
            &args,
            &types::Logicaltype::Complex("int-span".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 654usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_intersection",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 655usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_make (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_make".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_make",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 656usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tstzspan".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_midpoint (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_midpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_midpoint",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 657usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_minus (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_minus".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_minus",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 658usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 659usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_scale (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_scale",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 660usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_shift (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_shift".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_shift",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 661usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tstzspan".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_start (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_start".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_start",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 662usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("int-span".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 663usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tstzspan".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_union (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspan_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_union",
            &args,
            &types::Logicaltype::Complex("tstzspan".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 664usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspanset_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspanset_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspanset_from_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 665usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("tstzspanset_to_text (sqlink-shim-codegen)".into()),
            tags: vec!["tstzspanset_to_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "tstzspanset_to_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 666usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_always_eq (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_always_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_always_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 667usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_at_period (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_at_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_at_period",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 668usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_at_value (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_at_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_at_value",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 669usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_btrim (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_btrim".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_btrim",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 670usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_concat".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 671usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat_str (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_concat_str".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat_str",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 672usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_duration (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_duration".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_duration",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 673usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_end_timestamp (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_end_timestamp".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_end_timestamp",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 674usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_end_value (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_end_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_end_value",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 675usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_ever_eq (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_ever_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ever_eq",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 676usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_from_csv (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_from_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_from_csv",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 677usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_ilike (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_ilike".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ilike",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 678usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_left (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_left".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_left",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 679usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_length (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_length",
            &args,
            &types::Logicaltype::Complex("tint-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 680usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_like (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_like".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_like",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 681usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_lower (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_lower".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_lower",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 682usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_ltrim (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_ltrim".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ltrim",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 683usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_minus_period (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_minus_period".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_minus_period",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 684usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_minus_value (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_minus_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_minus_value",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 685usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_num_instants (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_num_instants".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_num_instants",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 686usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_num_instants_scalar (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_num_instants_scalar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_num_instants_scalar",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 687usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_prepend_str (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_prepend_str".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_prepend_str",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 688usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_regex_match (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_regex_match".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_regex_match",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 689usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_regex_replace (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_regex_replace".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_regex_replace",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 690usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_right (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_right".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_right",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 691usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_rtrim (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_rtrim".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_rtrim",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 692usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_start_timestamp (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_start_timestamp".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_start_timestamp",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 693usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_start_value (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_start_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_start_value",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 694usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_time_span (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_time_span".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_time_span",
            &args,
            &types::Logicaltype::Complex("time-period".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 695usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_to_csv (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_to_csv".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_to_csv",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 696usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_to_json (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_to_json".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_to_json",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 697usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("ttext-sequence".into()),
        }];
        let opts = runtime::Funcopts {
            description: Some("ttext_upper (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_upper".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_upper",
            &args,
            &types::Logicaltype::Complex("ttext-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 698usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_value_at (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_value_at".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_value_at",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 699usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_when_eq (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_when_eq".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_when_eq",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 700usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("ttext_when_ne (sqlink-shim-codegen)".into()),
            tags: vec!["ttext_when_ne".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "ttext_when_ne",
            &args,
            &types::Logicaltype::Complex("tbool-sequence".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 701usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("twed_distance (sqlink-shim-codegen)".into()),
            tags: vec!["twed_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "twed_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 702usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("twed_distance_normalized (sqlink-shim-codegen)".into()),
            tags: vec!["twed_distance_normalized".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "twed_distance_normalized",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 703usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("stbox3d_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["stbox3d_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "stbox3d_from_text",
            &args,
            &types::Logicaltype::Complex("stbox3d".into()),
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_aggregates() -> Result<(), types::Duckerror> {
    let capability =
        runtime::get_capability(types::Capabilitykind::Aggregate).ok_or_else(|| {
            types::Duckerror::Internal("host did not expose aggregate capability".into())
        })?;
    let registry = match capability {
        runtime::Capability::Aggregate(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "aggregate capability returned unexpected variant".into(),
            ));
        }
    };
    Ok(())
}

fn register_tables() -> Result<(), types::Duckerror> {
    let capability = runtime::get_capability(types::Capabilitykind::Table)
        .ok_or_else(|| types::Duckerror::Internal("host did not expose table capability".into()))?;
    let registry = match capability {
        runtime::Capability::Table(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "table capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("as_of_join_bool", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("as_of_join_float", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("as_of_join_int", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Text,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("as_of_join_text", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register(
            "interval_tree_query_overlapping",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("interval_tree_query_point", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("kdtree_xy_nearest_k", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("kdtree_xy_within", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("kdtree_xyz_nearest_k", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("kdtree_xyz_within", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("nearest_join_bool", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("nearest_join_float", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("nearest_join_int", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Text,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("nearest_join_text", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("octree_query_box", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("octree_query_sphere", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "value".into(),
            logical: types::Logicaltype::Int64,
        }];
        registry.register("quadtree_query_box", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tfloat-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "seq-id".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "json".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("sequence_json", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register(
            "spatial_index_find_in_envelope",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register(
            "spatial_index_find_within_distance",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("spatial_index_nearest", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("stindex_nearest", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("spatial_index_nearest_k", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "distance".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("stindex_nearest_k", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("time-period".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "sequence-id".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "segment-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "min-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "min-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "start-time".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "end-time".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("stindex_find_in_period", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "sequence-id".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "segment-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "min-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "min-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "start-time".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "end-time".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("stindex_find_in_spatial", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("stbox".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "sequence-id".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "segment-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "min-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "min-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "max-y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "start-time".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "end-time".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("stindex_find_in_stbox", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tcbuffer-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "radius".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("tcbuffer_to_rows", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("temporal_index_find_after", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("temporal_index_find_at", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register(
            "temporal_index_find_before",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register(
            "temporal_index_find_in_range",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "instant-index".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register(
            "temporal_index_find_overlapping",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tbool-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("temporal_join_bool", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("temporal_join_float", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Int64,
            },
        ];
        registry.register("temporal_join_int", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("ttext-sequence".into()),
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "left".into(),
                logical: types::Logicaltype::Text,
            },
            runtime::Columndef {
                name: "right".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("temporal_join_text", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "bucket-start".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "bucket-end".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "sub-sequence-json".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("tfloat_time_split", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tfloat-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "bucket-lower".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "bucket-upper".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "sub-sequence-json".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("tfloat_value_split", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeogpoint-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "lon".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "lat".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("tgeogpoint_to_rows", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint3d-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "z".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("tgeompoint3d_to_rows", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tgeompoint-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("tgeompoint_to_rows", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "bucket-start".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "bucket-end".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "sub-sequence-json".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("tint_time_split", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Complex("tint-sequence".into()),
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "bucket-lower".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "bucket-upper".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "sub-sequence-json".into(),
                logical: types::Logicaltype::Text,
            },
        ];
        registry.register("tint_value_split", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Complex("tpose-sequence".into()),
        }];
        let columns: Vec<runtime::Columndef> = vec![
            runtime::Columndef {
                name: "timestamp".into(),
                logical: types::Logicaltype::Int64,
            },
            runtime::Columndef {
                name: "x".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "y".into(),
                logical: types::Logicaltype::Float64,
            },
            runtime::Columndef {
                name: "orientation".into(),
                logical: types::Logicaltype::Float64,
            },
        ];
        registry.register("tpose_to_rows", &args, &columns, callback, None)?;
    }
    Ok(())
}

fn register_casts() -> Result<(), types::Duckerror> {
    {
        // CAST(<castsourcekind::stringliteral> AS BITEMPORAL_BOOL) -> bitemporal_bool_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "BITEMPORAL_BOOL".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(bitemporal_bool_from_ewkt: VARCHAR -> BITEMPORAL_BOOL): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS BITEMPORAL_FLOAT) -> bitemporal_float_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "BITEMPORAL_FLOAT".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(bitemporal_float_from_ewkt: VARCHAR -> BITEMPORAL_FLOAT): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS BITEMPORAL_INT) -> bitemporal_int_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "BITEMPORAL_INT".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(bitemporal_int_from_ewkt: VARCHAR -> BITEMPORAL_INT): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS BITEMPORAL_TEXT) -> bitemporal_text_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 47usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "BITEMPORAL_TEXT".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(bitemporal_text_from_ewkt: VARCHAR -> BITEMPORAL_TEXT): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS DATESPAN) -> datespan_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 88usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "DATESPAN".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(datespan_from_text: VARCHAR -> DATESPAN): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS DATESPANSET) -> datespanset_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 99usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "DATESPANSET".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(datespanset_from_text: VARCHAR -> DATESPANSET): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS FLOATSPAN) -> floatspan_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 150usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "FLOATSPAN".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(floatspan_from_text: VARCHAR -> FLOATSPAN): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS FLOATSPANSET) -> floatspanset_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 164usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "FLOATSPANSET".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(floatspanset_from_text: VARCHAR -> FLOATSPANSET): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS INTSPAN) -> intspan_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "INTSPAN".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(intspan_from_text: VARCHAR -> INTSPAN): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS INTSPANSET) -> intspanset_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 223usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "INTSPANSET".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(intspanset_from_text: VARCHAR -> INTSPANSET): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS STBOX) -> stbox_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 259usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "STBOX".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(stbox_from_text: VARCHAR -> STBOX): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS STBOX3D) -> stbox3d_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 703usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "STBOX3D".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(stbox3d_from_text: VARCHAR -> STBOX3D): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TBOX) -> tbox_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 311usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TBOX".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tbox_from_text: VARCHAR -> TBOX): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TCBUFFER) -> tcbuffer_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TCBUFFER".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tcbuffer_from_ewkt: VARCHAR -> TCBUFFER): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TCBUFFER_SEQUENCE) -> tcbuffer_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TCBUFFER_SEQUENCE".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tcbuffer_from_ewkt: VARCHAR -> TCBUFFER_SEQUENCE): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TEXTSET) -> textset_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 345usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TEXTSET".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(textset_from_text: VARCHAR -> TEXTSET): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TGEOMPOINT3D) -> tgeompoint3d_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TGEOMPOINT3D".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tgeompoint3d_from_ewkt: VARCHAR -> TGEOMPOINT3D): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TGEOMPOINT3D_SEQUENCE) -> tgeompoint3d_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TGEOMPOINT3D_SEQUENCE".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tgeompoint3d_from_ewkt: VARCHAR -> TGEOMPOINT3D_SEQUENCE): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TNPOINT) -> tnpoint_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TNPOINT".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tnpoint_from_ewkt: VARCHAR -> TNPOINT): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TNPOINT_SEQUENCE) -> tnpoint_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TNPOINT_SEQUENCE".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tnpoint_from_ewkt: VARCHAR -> TNPOINT_SEQUENCE): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TPOSE) -> tpose_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TPOSE".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tpose_from_ewkt: VARCHAR -> TPOSE): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TPOSE_SEQUENCE) -> tpose_from_ewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TPOSE_SEQUENCE".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tpose_from_ewkt: VARCHAR -> TPOSE_SEQUENCE): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TSTZSPAN) -> tstzspan_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 653usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TSTZSPAN".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tstzspan_from_text: VARCHAR -> TSTZSPAN): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS TSTZSPANSET) -> tstzspanset_from_text
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 664usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "TSTZSPANSET".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(tstzspanset_from_text: VARCHAR -> TSTZSPANSET): {}",
                e
            ))
        })?;
    }
    Ok(())
}

bindings::export!(MobilitydbBridge with_types_in bindings);
